# 项目协作规则

本文件适用于整个仓库。处理本项目时，优先遵守这里的规则。

## 全局工作台

- 开始新项目或整理项目资料前，先读取 `Workspace/00_GLOBAL_WORKBENCH.md`。
- 每个新项目必须在 `Workspace/projects/` 下创建 `projectNNN_项目名称` 文件夹。
- 项目相关输入、中间文件、输出、日志必须放入对应项目文件夹，不能散落到电脑其它位置。
- 犯错、踩坑、绕路、形成可复用方法时，更新 `Workspace/01_COMPOUND_LESSONS_LOG.md`。
- 新项目创建和归档流程按 `Workspace/02_NEW_PROJECT_SOP.md` 执行。

## 核心原则

- 从目标需求出发。先确认要解决的问题、完成条件和验证方式。
- 不隐藏困惑。需求存在多种合理解释、重要取舍或潜在风险时，先说明并确认。
- 优先选择最简单、最直接的实现。不要增加没有被要求的功能、配置或抽象。
- 只修改任务需要的代码。不要顺手重构、格式化或清理无关区域。
- 尽早暴露错误。不要用静默降级、猜测拆分或不严谨的兜底逻辑掩盖问题。
- 每次改动后先 Review 查 Bug，再做一遍梳理分析，确认是否有更简单、更可靠的实现。

## 编码要求

- 保持改动范围小，每一处变化都应能对应到当前任务。
- 单次使用的逻辑不要提前抽象。只有确实减少复杂度或重复时才新增抽象。
- 匹配仓库现有风格。不要因为个人偏好改动邻近代码。
- 只清理本次改动产生的无用代码。发现原有无关问题时记录下来，不要擅自删除。
- 修复 Bug 时，优先补充可复现问题的测试，再让测试通过。
- 重构时，必须确认改动前后的行为和测试结果一致。

## 完成标准

开始多步骤任务前，用简短计划写清楚：

```text
1. 要做什么 -> 如何验证
2. 要做什么 -> 如何验证
3. 要做什么 -> 如何验证
```

任务完成前必须：

- 运行与改动范围匹配的测试。
- 检查是否引入无关修改。
- 检查是否存在更简单的实现。
- 更新 `context.md`，用极简方式记录阶段进度和关键决定。

## 沟通方式

- 使用简洁、直白的中文。
- 把用户当作非技术背景也能理解的协作者，少用术语堆砌。
- 明确的子任务可以自主完成，不需要逐步确认。
- 只有遇到真正影响结果的决策分叉时才停下来询问。
- 最终总结只说明做了什么、结果是什么，以及必须注意的问题。

## Karpathy Guidelines

本项目采用 `karpathy-guidelines` 的四条原则：

1. Think Before Coding：不要默默假设，主动说明困惑、取舍和风险。
2. Simplicity First：使用解决当前问题所需的最少代码，不做推测性设计。
3. Surgical Changes：只修改必要范围，只清理自己产生的无用代码。
4. Goal-Driven Execution：先定义可验证的完成条件，循环执行直到验证通过。

参考来源：`multica-ai/andrej-karpathy-skills`，MIT License。
