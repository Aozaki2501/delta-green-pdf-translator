# 原版 PDF 复刻排版开发计划

## 目标

把“原版坐标 PDF”从现在的坐标草稿，改成接近人工排版流程的可检查中间稿。

最终路线不是直接从译文生成最终 PDF，而是生成一个 HTML 中间排版稿：

- 每页保留原 PDF 页面尺寸。
- 每页使用原 PDF 渲染图作为底图。
- 中文译文作为可调整文本层覆盖在原位置。
- 浏览器负责真实排版、自动缩字和导出 PDF。
- 人可以先检查 HTML，再导出 PDF。

## 现状

当前已经支持：

- 提取 PDF 文本块坐标。
- 按文本块翻译。
- 回填译文。
- 生成 HTML 检查稿。
- 浏览器导出 PDF。
- 浏览器阶段自动缩小译文字号。

当前主要问题：

- 没有把原 PDF 页面图像嵌回去，整页图片页会变成空白。
- 背景、照片、纸张纹理、装饰色块都丢失。
- 中文文本只按坐标覆盖，复杂页面看起来像白纸草稿。
- 溢出报告仍来自保守检测，只能作为提示，不能代表最终浏览器排版结果。

## 开发计划

### 1. 生成原页底图

- 从原 PDF 按页渲染 PNG 或 WebP。
- 输出到当前任务目录的 `assets/replica_pages/`。
- 文件名包含源页码，保证可追溯。
- 底图尺寸和 PDF 页面尺寸严格对应。

### 2. 扩展 layout JSON

- 在 `LayoutPage` 里记录底图路径。
- 保留原有文本块、图片块、页面尺寸字段。
- 旧 layout JSON 缺少底图字段时直接报错，不做静默降级。

### 3. 生成 HTML 中间排版稿

- 每个 `.replica-page` 先放底图。
- 中文文本层覆盖在原坐标位置。
- 保留 `data-block-id`，便于人工定位问题。
- 检查模式显示文本框、缩字结果和溢出状态。

### 4. 处理原文遮盖

第一阶段使用保守遮盖：

- 在中文文本框下面放白色或接近纸张颜色的遮罩。
- 遮罩只覆盖原文字区域，不覆盖整页。

第二阶段再做精细处理：

- 根据页面底色采样决定遮罩颜色。
- 对复杂背景允许半透明遮罩。
- 图片页和装饰页优先保持底图，不强行清除英文。

### 5. 浏览器排版检查

- PDF 导出前执行浏览器内的 `replicaFitTranslations()`。
- 收集每个文本块的最终字号和是否仍然溢出。
- 写出浏览器真实排版报告，替代现在的保守溢出报告。

### 6. Web 流程调整

- “原版坐标 PDF”先生成 HTML 中间稿。
- 界面明确下载：
  - HTML 排版稿
  - PDF 成品
  - 浏览器排版报告
  - layout JSON
  - translations JSON
- 有浏览器真实溢出时，PDF 仍可生成，但审计记录标注需要人工检查。

## 完成标准

- 整页图片页不再空白。
- 有背景和照片的页面能保留原视觉主体。
- 译文能覆盖到原文本区域。
- HTML 中间稿可以作为人工检查入口。
- PDF 由 HTML 中间稿导出，结果和浏览器预览一致。
- 测试覆盖底图路径、HTML 输出、PDF 导出和排版报告。
