# 原版 PDF 复刻排版开发计划

## 目标

把“原版坐标 PDF”从现在的坐标草稿，改成接近人工排版流程的可检查中间稿。

最终路线不是直接从译文生成最终 PDF，而是生成一个 HTML 中间排版稿：

- 每页保留原 PDF 页面尺寸。
- 每页使用原 PDF 渲染图作为底图。
- 中文译文作为可调整文本层覆盖在原位置。
- 浏览器负责真实排版、自动缩字和导出 PDF。
- 人可以先检查 HTML，再导出 PDF。

## 现状

当前已经支持：

- 提取 PDF 文本块坐标。
- 按文本块翻译。
- 回填译文。
- 生成 HTML 检查稿。
- 浏览器导出 PDF。
- 浏览器阶段自动缩小译文字号。

当前主要问题：

- 没有把原 PDF 页面图像嵌回去，整页图片页会变成空白。
- 背景、照片、纸张纹理、装饰色块都丢失。
- 中文文本只按坐标覆盖，复杂页面看起来像白纸草稿。
- 溢出报告仍来自保守检测，只能作为提示，不能代表最终浏览器排版结果。

## 开发计划

### 阶段 1：原页底图闭环

目标：让 HTML 检查稿不再是白纸草稿，而是有原 PDF 页面底图。

- 从原 PDF 按页渲染 PNG 或 WebP。
- 输出到当前任务目录的 `assets/replica_pages/`。
- 文件名包含源页码，保证可追溯。
- 底图尺寸和 PDF 页面尺寸严格对应。

- 在 `LayoutPage` 里记录底图路径。
- 保留原有文本块、图片块、页面尺寸字段。
- 旧 layout JSON 缺少底图字段时直接报错，不做静默降级。

- 每个 `.replica-page` 先放底图。
- 中文文本层覆盖在原坐标位置。
- 保留 `data-block-id`，便于人工定位问题。
- 检查模式显示文本框、缩字结果和溢出状态。

验收：

- `extract_layout.py` 会生成 `layout.json` 和页面底图。
- `render_layout_html.py` 输出的 HTML 能看到原页底图。
- Web 的“原版坐标 PDF”同样输出带底图的 HTML 和 PDF。
- 测试覆盖底图字段、缺字段报错、HTML 引用底图。

### 阶段 2：原文遮盖

第一阶段使用保守遮盖：

- 在中文文本框下面放白色或接近纸张颜色的遮罩。
- 遮罩只覆盖原文字区域，不覆盖整页。

第二阶段再做精细处理：

- 根据页面底色采样决定遮罩颜色。
- 对复杂背景允许半透明遮罩。
- 图片页和装饰页优先保持底图，不强行清除英文。

验收：

- 译文下方能遮住主要英文原文。
- 遮罩不覆盖整页，不破坏图片页主体。
- 检查模式能看出遮罩区域。

### 阶段 3：浏览器排版检查

- PDF 导出前执行浏览器内的 `replicaFitTranslations()`。
- 收集每个文本块的最终字号和是否仍然溢出。
- 写出浏览器真实排版报告，替代现在的保守溢出报告。

验收：

- 导出 PDF 前生成浏览器真实排版报告。
- 报告记录块 ID、页码、最终字号、是否溢出。
- Web 审计记录使用浏览器报告，而不是只用保守估算。

### 阶段 4：Web 流程调整

- “原版坐标 PDF”先生成 HTML 中间稿。
- 界面明确下载：
  - HTML 排版稿
  - PDF 成品
  - 浏览器排版报告
  - layout JSON
  - translations JSON
- 有浏览器真实溢出时，PDF 仍可生成，但审计记录标注需要人工检查。

验收：

- 页面上能直接下载 HTML 中间稿、PDF、排版报告和两个 JSON。
- Web 文案明确这个模式需要人工检查。
- 普通阅读版输出不受影响。

## 完成标准

- 整页图片页不再空白。
- 有背景和照片的页面能保留原视觉主体。
- 译文能覆盖到原文本区域。
- HTML 中间稿可以作为人工检查入口。
- PDF 由 HTML 中间稿导出，结果和浏览器预览一致。
- 测试覆盖底图路径、HTML 输出、PDF 导出和排版报告。
## 最终成品排版路线

### 核心判断

原坐标 PDF 不是最终成品，而是中间检查稿。

它的作用是：
- 保留原 PDF 页面坐标。
- 验证翻译块能否回到原位置。
- 暴露双栏、图片、标题、遮盖、溢出问题。
- 给后续自动排版系统提供可追踪的页面结构。

最终成品路线不是继续堆白色遮盖框，而是从原坐标中间稿升级为分层中文排版稿。

### 最终目标

生成接近《绿色三角洲-甜心.pdf》水准的中文 PDF：
- 正文双栏稳定。
- 标题、页眉、页脚、注释、图片位置稳定。
- 图片和背景保留原视觉。
- 英文正文不再明显残留。
- 复杂页允许标记为人工校正页。

### 阶段 5：页面分类

目标：每页先判断适合哪种排版策略。

页面类型：
- `body_two_column`：普通双栏正文页。
- `body_with_image`：正文加图片页。
- `title_or_cover`：封面、标题页。
- `decorative_document`：斜放纸张、档案、便签页。
- `full_art`：整页插图页。
- `table_or_sheet`：表格、角色卡、清单页。

验收：
- 每页 layout JSON 写入 `page_type`。
- Web 审计显示页面类型统计。
- 复杂页不会被普通正文规则硬套。

### 阶段 6：正文双栏重排

目标：先把最常见正文页做成稳定成品。

做法：
- 根据原坐标识别左右栏。
- 译文按栏重新排文，不再逐块硬塞。
- 标题块、正文块、列表块分开处理。
- 页眉页脚独立渲染。
- 溢出时报错并写报告，不自动乱缩。

验收：
- 双栏正文页接近人工排版效果。
- 左右栏阅读顺序正确。
- 页眉页脚不压正文。

### 阶段 7：图片和背景分层

目标：图片页不再只靠整页底图。

做法：
- 提取可独立保存的图片对象。
- 背景纹理保留为底层。
- 正文区域尽量清成纸面色。
- 图片按原位置放回。
- 不能可靠分离的页面标记为人工校正。

验收：
- 图片不被白框误盖。
- 正文区域英文残留明显减少。
- 图片正文混排页可读且接近原视觉。

### 阶段 8：标题、注释和装饰元素

目标：补齐《甜心》这类成品的版式细节。

做法：
- 红色小标题、粗标题、分割线独立样式。
- 注释、脚注、小字块独立样式。
- 页眉横线、页码、书名栏独立渲染。
- 列表缩进和项目符号固定规则。

验收：
- 正文页不再像“网页文字盖 PDF”。
- 标题层级和注释层级清楚。

### 阶段 9：复杂页策略

目标：封面、斜纸张、档案页不再被普通遮盖框破坏。

做法：
- 支持文字块旋转角度。
- 对斜放纸张类页面使用专门模板。
- 对封面标题使用少量大块替换。
- 无法稳定自动处理的页面输出校正提示。

验收：
- 封面和标题页不出现横平竖直的大白框。
- 复杂页至少不破坏原图主体。

### 阶段 10：人工校正文件

目标：自动排版之后允许小范围人工微调。

做法：
- 新增 `layout_overrides.json`。
- 支持修改块位置、宽度、字号、隐藏、页面类型。
- 支持重新导出，不重新翻译。

验收：
- 修改校正文件后可快速重出 HTML/PDF。
- 不需要改代码就能修单页排版。

### 阶段 11：成品导出

目标：把中间稿和成品稿分开。

输出分为：
- `_replica.html/pdf`：原坐标中间检查稿。
- `_typeset.html/pdf`：中文排版成品稿。
- `_layout_report.md`：排版报告。
- `_overrides.json`：人工校正文件。

验收：
- Web 下载区只显示最终成品。
- 中间文件保留在本地，供排查和续跑使用。
