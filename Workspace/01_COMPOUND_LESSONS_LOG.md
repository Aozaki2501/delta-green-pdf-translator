# 全局复利与踩坑日志

本文件用于记录犯过的错误、踩过的坑、走过的弯路，以及未来可以复用的方法。

## 使用规则

1. 不记录流水账，只记录能避免未来重复犯错的内容。
2. 每条记录要写清楚：发生了什么、为什么错、怎么修、以后怎么避免。
3. 如果某条经验会影响新项目流程，要同步更新 `02_NEW_PROJECT_SOP.md`。
4. 如果某条经验会影响 Codex 行为，要同步更新 `00_GLOBAL_WORKBENCH.md`。

## 记录模板

```text
### YYYY-MM-DD｜标题

- 场景：
- 问题：
- 原因：
- 修正：
- 以后复用的方法：
- 需要同步更新的文档：
```

## 日志

### 2026-06-16｜失败页与重翻页被互相覆盖

- 场景：PDF 翻译项目中，同时勾选“只重试失败页”并填写手动重翻页。
- 问题：手动重翻页被清掉旧译文，但没有进入翻译队列。
- 原因：任务页选择只取失败页，没有把失败页和重翻页合并。
- 修正：把实际任务页改为失败页与重翻页的并集。
- 以后复用的方法：凡是两个筛选条件都可能产生任务页时，先明确它们是交集、并集还是覆盖关系，再写代码。
- 需要同步更新的文档：无。

### 2026-06-16｜普通加号被误判成表格分隔线

- 场景：PDF 提取中，`10+ hours`、`4+ Health damage` 这类正文被识别成表格。
- 问题：Word/HTML 里普通段落被拆成表格。
- 原因：表格分隔线规则把普通 `+` 当成了强表格信号。
- 修正：收紧表格分隔线判断，普通加号不再触发表格。
- 以后复用的方法：格式识别只能使用强信号；普通正文符号不能单独作为结构判断依据。
- 需要同步更新的文档：无。

### 2026-06-16｜目录代码围栏挤在同一行导致泄漏

- 场景：目录页译文出现 `标题```toc条目` 挤在同一行。
- 问题：HTML/Word 导出没有识别目录围栏，导致 ` ```toc ` 显示到正文。
- 原因：导出器只识别独占一行的目录围栏。
- 修正：导出前规范化内联目录围栏，把它拆回标准多行格式。
- 以后复用的方法：导出器需要先做标记规范化，再做结构渲染。
- 需要同步更新的文档：无。

### 2026-06-17｜PowerShell 读中文文件要指定 UTF-8

- 场景：检查 `context.md` 和全局工作台文档。
- 问题：中文内容显示成乱码。
- 原因：PowerShell 没有按 UTF-8 读取 Markdown。
- 修正：改用 `Get-Content -Encoding UTF8` 重新读取。
- 以后复用的方法：读取项目中文 Markdown 时明确指定 UTF-8。
- 需要同步更新的文档：无。

### 2026-06-17｜上传文件用随机名会重复占空间

- 场景：Web 端多次上传同一本 PDF。
- 问题：同一 PDF 被保存成多个随机文件，`uploads/` 快速膨胀。
- 原因：上传文件名使用 uuid 后缀，没有按内容去重。
- 修正：保存上传文件前计算 SHA256，已存在相同内容时直接复用。
- 以后复用的方法：大文件缓存应优先按内容 hash 去重，随机名只适合临时小文件。
- 需要同步更新的文档：无。

### 2026-06-25｜PowerShell 后台服务日志不能重定向到同一文件

- 场景：启动 Streamlit 做界面截图检查。
- 问题：`Start-Process` 同时把 stdout 和 stderr 指向同一个日志文件时直接失败。
- 原因：PowerShell 不允许 `RedirectStandardOutput` 和 `RedirectStandardError` 使用相同目标。
- 修正：分别写入 `.out.log` 和 `.err.log`。
- 以后复用的方法：Windows 后台服务启动时，stdout/stderr 分开重定向；需要合并时后续再读两个文件。
- 需要同步更新的文档：无。

### 2026-06-25｜Playwright 浏览器版本要匹配调用端

- 场景：用 `node_repl` 调 Playwright 做 Streamlit 截图检查。
- 问题：项目虚拟环境装过 Chromium，但 `node_repl` 仍提示找不到浏览器。
- 原因：Python Playwright、npx Playwright 和 `node_repl` 内置 Playwright 版本不同，浏览器目录不通用。
- 修正：先读取 `node_repl` 中 Playwright 的版本，再用 `npx playwright@版本 install chromium` 安装匹配浏览器。
- 以后复用的方法：截图检查前先确认实际调用端的 Playwright 版本，不要假设其它环境装过的浏览器可复用。
- 需要同步更新的文档：无。

### 2026-07-10｜网页初始化器缺少 Node 运行时

- 场景：在 Windows 上创建独立网页样例。
- 问题：初始化器已复制模板，但 npm 安装阶段失败。
- 原因：本机没有 Node/npm，工作区也没有配置 bundled runtime。
- 修正：保留独立目录，改用纯 HTML/CSS/JavaScript 完成交互原型，并清理未使用模板。
- 以后复用的方法：初始化失败时不重复运行初始化器，也不擅自安装系统运行时；先判断静态原型能否满足目标。
- 需要同步更新的文档：`Workspace/projects/project001_frontend_mockup/04_logs/setup_issue.md`。
# 2026-07-11：原型确认后必须再次确认实施目标

- 场景：用户先要求独立网页样例，确认方向后要求开始改动前端。
- 问题：误把“开始改动前端”理解为继续修改独立样例，没有改真实项目。
- 原因：沿用了上一阶段的安全边界，没有根据“开始改动”重新确认交付对象。
- 修正：将视觉方案接入真实 `app.py` 和 `webui/theme.py`，独立样例只保留为参考。
- 以后复用的方法：原型阶段转实施阶段时，明确写出目标仓库、入口文件和是否接入业务，再开始修改。

# 2026-07-11：Streamlit 主题模块改动后重启服务

- 场景：真实页面结构已更新，但新 CSS 类在浏览器中没有生效。
- 问题：页面显示新 HTML，却仍使用旧的主题模块内容。
- 原因：运行中的 Streamlit 进程保留了已导入的 `webui.theme` 模块。
- 修正：确认端口对应 Streamlit 后重启服务，再做截图检查。
- 以后复用的方法：修改 Streamlit 导入模块中的 CSS 或组件后，验证前先重启服务，不能只依赖页面热刷新。

# 2026-07-11：界面还原必须包含操作结构

- 场景：用户要求把真实项目还原成已确认的网页样例。
- 问题：虽然加入了侧栏导航和流程条，任务参数和启动区仍沿用旧结构，使用感没有根本变化。
- 原因：把视觉层级重构误当成新增外框，没有逐项对照样例中的控件位置和操作顺序。
- 修正：按截图圈选区域验收，移除侧栏任务表单，参数移入第二步，重做第四步预检与启动。
- 以后复用的方法：从原型迁移到真实页面时，逐项核对导航、控件归属、步骤切换、主按钮位置和空白关系，不能只检查配色与标题。

# 2026-07-14：前端重构必须覆盖运行态

- 场景：新建任务页面已经重构，但开始翻译后又出现旧黑色终端组件和重复状态块。
- 问题：首页好看，真正工作时仍然拥挤、割裂，并产生横向滚动。
- 原因：只验收了任务创建阶段，没有按完整翻译流程检查组件的持续更新方式。
- 修正：档案卡、阶段条和日志改为固定位置更新，并统一使用新工作台视觉。
- 以后复用的方法：工作台改版必须走完空状态、已上传、执行中、失败、完成五种状态；动态信息默认原位更新，不逐次追加。

# 2026-07-14：浅色主题必须清除旧控件状态

- 场景：新版浅色工作台中的多选框和数字按钮仍显示旧黑绿样式。
- 问题：深色底配深绿色文字，关键设置几乎看不清。
- 原因：只覆盖了输入框外层，没有覆盖标签、步进按钮、图标和滑杆状态。
- 修正：对这些内部控件逐项设置浅色背景和高对比前景色。
- 以后复用的方法：主题切换要逐项检查输入、标签、图标、悬停、聚焦和禁用状态，不能只看控件外框。

# 2026-07-14：审查产物时不能引用界面样例数据

- 场景：检查 Holy War 原 PDF 与翻译产物。
- 问题：一开始把此前界面样例中的 184 页误当成真实文件页数。
- 原因：在直接读取 PDF 前使用了截图里的演示数字。
- 修正：用 PyMuPDF 读取原文件，确认真实页数为 110，并立即更正结论。
- 以后复用的方法：页数、文件范围和输出对应关系必须直接读取目标文件与 manifest，不能引用原型或截图中的示例值。

# 2026-07-14：PowerShell 管道中的弯引号要用 Unicode 转义验证

- 场景：验证 `Y’golonac` 术语别名。
- 问题：Python 校验脚本中的弯引号经 PowerShell 管道后被错误编码，首次断言误报。
- 原因：控制台输出编码不等于脚本文本输入编码。
- 修正：验证脚本使用 `\u2019` 表示弯引号，并分别测试每个别名。
- 以后复用的方法：通过 PowerShell 向解释器传递非 ASCII 标点时，关键字符使用 Unicode 转义，避免编码误判。

# 2026-07-16：宽屏主题不能用整屏负边距

- 场景：扩大 Streamlit 主内容区后检查 2048px 宽屏。
- 问题：顶部密级条的整屏负边距让页面视觉范围异常，内容区也显得过宽。
- 原因：旧规则假定页面没有固定侧栏，和当前工作台布局不兼容。
- 修正：密级条限制在主容器内，内容区最大宽度收紧到 1440px。
- 以后复用的方法：带固定侧栏的页面不要用 `50vw` 负边距模拟全宽；宽屏验收同时检查视口宽度和页面滚动宽度。
