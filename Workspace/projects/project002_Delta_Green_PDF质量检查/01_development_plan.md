# 修复计划

1. 修正正文路由与英文回退 -> 第 2、5、15 页的中文内容均被渲染，HTML 不再输出被翻译块的英文原文。
2. 增加内容完整性与底图残留校验 -> 正文遗漏、英文底图残留、文字溢出任一出现即终止导出。
3. 调整双栏排版与译文长度约束 -> 第 6、9、14 页无溢出，且不缩小到不可读。
4. 重建前 15 页并生成 PDF -> HTML、PDF 均通过硬校验。
5. 逐页视觉验收 -> 15 页截图无缺字、叠字、英文残留和溢出。

## 第二阶段：排版细节修复

1. 补齐中文行高、标题、标签和续段的测试 -> 失败样例可稳定复现。
2. 按文本语义修复渲染规则 -> 不为单个英文词或单页写特例。
3. 修正文档标题与产物命名 -> 新产物没有哈希标题和重复“fixed”文件。
4. 全量测试与浏览器渲染检查 -> 无回归后提交并推送 main。
