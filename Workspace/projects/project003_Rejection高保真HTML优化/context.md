# 当前计划

1. 固定英文原件、第一版中文 HTML 与《融合》排版参考 -> 已完成
2. 修状态、内容归属、候选发布和普通文档成品安全 -> 已完成
3. 修 5 处溢出、遮挡重复文字、旋转和强调样式 -> 已完成
4. 逐页视觉复核、全量测试和无关差异检查 -> 已完成

# 当前进度

- 已确认 Rejection 没有人工汉化版；当前 HTML 是第一版中文内容，《融合》只作排版参考，《复疾》已排除。
- 候选版 25 页：浏览器溢出 0、坏图 0、预期文字块 445/445、重复归属 0、错页 0。
- 第 3 页照片下方重复标题已隐藏；斜排纸张保持原角度。
- 整段斜体保留；旧译文中无法可靠还原的混排强调已进入质量报告，后续翻译会保留 `<em>/<strong>` 边界。
- 普通文档不再猜测跨页续句或删除重复句，HTML、Markdown、Word 与高保真产物统一采用候选文件发布。
- Playwright 最终验收：25 页、445/445 译文块、0 溢出、0 坏图、0 缺失、0 重复、0 错页，字体已加载。
- 全量测试 850 项通过，0 项失败；`git diff --check` 通过。

# 关键决定

- 从现有 HTML 和译文出发，不整本重翻。
- 不写单页硬编码补丁，只修可复用结构与渲染规则。
- 未通过浏览器验收的 HTML 不得覆盖正式成品。
- 不从字体比例、重复文本或断句形态猜测并改写译文；不确定项只报告，不自动修补。
- 现有 `app.py`、`webui/theme.py` 和根 `context.md` 有用户未提交改动，本项目不覆盖这些改动。
