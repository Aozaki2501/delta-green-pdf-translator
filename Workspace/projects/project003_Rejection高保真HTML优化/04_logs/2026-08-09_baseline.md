# 基线检查日志

## Playwright 本地文件限制

- 现象：`playwright-cli open file:///...` 被拒绝，提示 `Access to file: protocol is blocked`。
- 原因：Playwright CLI 的安全策略不允许直接访问本地文件协议。
- 修正：从只读输入目录启动本地 HTTP 服务，再通过 `http://127.0.0.1` 打开。
- 复用方法：后续本地 HTML 视觉验收统一使用独立端口的本地 HTTP 服务，不再尝试 `file:`。
