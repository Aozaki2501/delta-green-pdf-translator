# 本次检查：Rejection 排版溢出诊断（2026-08-08）

# 本次修复：红色标题整段变红与字体（2026-08-08）

1. 现象：同一区域被合并成自然流时，第一个块若是红色短标题，整段合并文本都被渲染成红色大标题；且红色标题同时带标题类和正文类，正文宋体把标题黑体覆盖。
2. 修复：`_render_same_region_flows` 拆分为标题块单独渲染、正文块合并为段落；accent 标题强制使用标题角色类（黑体），不再挂正文类。
3. 新增回归测试：长红色块保持正文、短红色块渲染为黑体标题；全量测试 828 个通过。
4. 当前 output 下的 Rejection HTML 仍是修复前旧产物，需重新运行生成流程验收。

# 本次开发：DG 标题保真与成组溢出修复（2026-08-08）

1. 已确认“大标题”和黑色小节标题来自原 PDF；原版红色短标题（如 The Gate / The Guard）曾被误判为正文，现保留原红色并渲染为小节标题。
2. 中文正文不再塞入英文原始行槽位；行槽的 `overflow:hidden` 曾吞掉放不下的文字。现在改为自然重排并将真实溢出交给 Chromium 验收。
3. 修复清单升级为共享容器组：同一区域全部块一次翻译、一次缓存、一次实测，禁止把同一高度错误分配给每个子块；浏览器反馈后最多自动再修一轮剩余组。
4. 用现有 Rejection 译文重建后，红色标题已视觉确认。布局报告现显示 60 个可见问题，这是取消静默截断后暴露出的真实容量问题；清单已生成 60 个可定位组、0 个未定位组，等待网页定向修复。
5. 全量测试 827 个通过。

# 本次开发：网页一键定向修复（2026-08-08）

1. 高保真 HTML 检查失败时，网页直接显示“修复溢出文字并重新检查”按钮；不再要求用户下载或上传 JSON 清单。
2. 修复按钮通过点击回调登记同一份 PDF 与排版配置；下一次页面运行直接读取本地实测清单并只重译指定溢出块，保留其余已有译文。
3. 每次修复后都会重新生成 HTML 并用 Chromium 验收；仍溢出时会更新清单和重试签名，避免复用上一次未通过的紧凑译文。
4. 已覆盖管线修复、清单拒绝和重试签名；全量测试 822 个通过。

# 本次开发：布局报告、结构模板与已有译文复用（2026-08-08）

1. 高保真重绘现在会保存完整 Chromium 布局报告：目标块、子块 ID、实际盒尺寸、滚动尺寸、溢出像素和页面边界信息；报告不再只显示前 5 条。
2. 已将 Rejection 第 39、47、53 页的卡片/统计/事件表按结构模板重排；第 39 页长卡片和第 53 页 Likely Events 表不再作为普通逐块定位流。
3. 已有译文可直接复用；溢出修复接口只接受明确的容量、模板签名和约束提示，只重译指定块，不会整本重翻。译文缓存按目标签名隔离。
4. `[damaged]` 在普通正文中不再被视为完成译文；第 15 页的 `M/X` 已确认来自前景表单图片，转为不可翻译图像文字并保留在视觉层，不再触发整本重翻。
5. 失败时另生成 `*_typeset_layout_repair_targets.json`，把实测容量映射到 40 个具体块，供人工复核后定向复译；不自动猜字符预算。
6. 本次离线结构回归从 43 处降至 16 处，剩余项已定位到具体块，等待容量约束下的定向复译；全量验收仍以 0 个布局问题和 0 个损坏占位为门槛。

1. Delta Green: Rejection 首次高保真排版在 Chromium 布局校验处失败：43 处内容溢出（均在页面边界内，是块内文字超过容器高度）。
2. 分布：25 个固定文本块（第 39 页 12、第 47 页 8）、14 个栏内流、2 个旋转流、时间轴与行槽流各 1。
3. 主因：中文译文行数超过源 PDF 固定区域容量（例：第 3 页一段英文 154 字符占 41.7pt，中文 72 字需约 72pt，区域只给 47.7pt）。
4. 异常项：第 15 页 [damaged] 占位（源字符不可读，窄块逐字换行溢出 98px）；第 61 页等 4 处以上译文混入繁体。
5. 处理选项：A) 用「重翻页码」重译涉及的 19 页（提示词无容量约束，重译未必收敛）；B) 改代码给翻译加区域容量预算或溢出块反馈重译。

# 本次更新：B7 CI / 依赖固定 / LICENSE 完成（2026-08-05）

1. 新增 `.github/workflows/ci.yml`，push/PR 自动跑全量测试；`requirements.txt` 全部固定精确版本，新增 `requirements.lock.txt` 锁定传递依赖。
2. LICENSE 你选了 Apache-2.0（第二个选项），已写入官方文本并保留版权人 chenzhiming_nshx。
3. 本地验证 802 测试通过；`google-genai` 可选化未做，属于独立小改造，不在 B7 范围。

# 本次更新：推送 main 并刷新优化进度（2026-08-05）

1. 已把 KULT 相关未提交改动（独立排版配置、翻译上下文签名、瑞典语术语表、T3 溢出检测、阅读页互链）提交为 `ef31634` 并推送到 main；`.obsidian/` 与 `tmp/` 加入 .gitignore。
2. 已更新 `docs/OPTIMIZATION_PROGRESS.md`：T3 溢出检测与 B9 残留清理标记完成，测试基线 802 通过。
3. 仍未完成：KULT 全书真实译文验收（需下一次带翻译接口的运行）。

# 本次检查：KULT 1-24 页高保真 HTML（2026-07-29）

1. 新输出已确认使用 `kult` 配置和瑞典语源语言；24 页、507 个文本块完整对应，0 个空翻译。
2. 已确认封面标题未渲染，第 5 页版权页严重重叠，第 6、11、24 页有裁切或页脚冲突，第 24 页伤害符号字体失效。
3. 译文仍有繁简混用、软连字符、瑞典语规则词和跨块断词；英文术语表不能约束瑞典语源文。
4. 固定页 HTML 适合桌面预览，不适合手机连续阅读；下一步应先修封面、专用页面模板、HTML 强制验收和符号映射，再处理瑞典语术语与阅读模式。

# 本次修复：KULT 1-24 页高保真 HTML（2026-07-29）

1. 已修复封面文字丢失、版权页重叠、Wingdings 符号失效；KULT 专用字号与行距使第 6、11 页无浏览器溢出。
2. 高保真 HTML 现在必须通过 Chromium 版面检查；固定页和阅读页会同时输出并互相链接。
3. KULT 已加入独立瑞典语术语表、软连字符清理、正文强调标记和翻译上下文签名；旧译文缓存不再会被错误复用。
4. 已用现有 24 页译文重建并验证固定页 0 个版面问题、手机阅读页和双向切换链接正常；完整重译将在下一次带翻译接口的运行中自动触发。

# 本次开发：KULT 章节与绕图模板（2026-07-29）

1. 已把章节开篇的双行装饰标题合并为一个稳定翻译块，并新增 `full_width_hero` 模板。
2. 已将首字下沉开篇段落固定为独立区域，避免它与标题或后续双栏正文互相覆盖。
3. 已为中央插画两侧的正文标记 `image_float`；渲染器继续按源页面逐行槽位排版，不将它误当作完整矩形双栏。
4. 第 6、7 页使用短中文占位文本在 Chromium 截图中无溢出；未调用翻译接口。
5. 图册页继续由现有 `ART` 固定视觉层处理，不生成正文栏。

# 本次开发：KULT / DG 高保真排版切换（2026-07-29）

1. 已新增 `delta_green` 与 `kult` 独立排版配置；界面在选中高保真输出时可切换，翻译提示、来源语言、字号、颜色与报告独立。
2. 已修复 KULT 第 70、100、101 页的图片身份解析；整本 120 页结构提取完成。
3. 已增加 KULT 红色正文识别和首字下沉合并；第 6 页正文不再逐行重叠。
4. KULT 章节开篇标题及绕图模板仍未完成；离线预览第 6 页仍有 2 处溢出，不可视为高保真交付。
5. 全量测试 784 个通过、0 个失败。

# 本次计划：KULT 高保真 HTML（2026-07-29）

1. 已确认《往昔回响》不是《Avgrunden》的逐页汉化版，只作为 KULT 中文字体、密度和主题参考。
2. 第一版以忠实《Avgrunden》原版页面结构为目标，不 OCR、不翻译插画文字；《往昔回响》式金黑主题留作可选模式。
3. 开发顺序确定为：图片提取硬错误 -> 游戏配置解耦 -> KULT 语义模型 -> KULT 渲染 -> 全书质量检查。
4. 代表页与验收标准已写入 `docs/KULT_HIGH_FIDELITY_PLAN.md`。

# 本次更新：同步远端 main（2026-07-29）

1. 已从 `759fe64` 快进至 `a8a8fa0`，包含模型价格默认值与高保真排版优化。
2. 本地未提交改动已完整恢复；仅测试文件出现冲突，已合并并保留工作区。
3. 相关测试 39 个通过、1 个失败；失败断言在更新前的本地暂存版本中已与当前实现不一致。

# 本次检查：KULT 通用高保真排版（2026-07-27）

1. 当前流程无法完整导出该 PDF：第 70、100、101 页已确认在图片身份校验处报错。
2. 现有语义和样式仍绑定 Delta Green；KULT 的首字下沉、绕图、装饰字体、图册页和瑞典语提示均未被正确建模。
3. 通用化方向确定为“统一核心 + 游戏配置”：核心负责页面结构、翻译、约束排版和质量检查，各规则配置负责术语、字体角色、颜色与特殊版式。
4. 本轮只做诊断，未修改业务代码，也未调用翻译接口。

# 本次修复：日文正文漏翻拦截（2026-07-27）

1. 译文含日文假名时现在直接失败，不会写入新进度或缓存。
2. 旧进度、旧缓存含日文假名时不再复用，将进入重译。
3. 已覆盖常规与高保真翻译流程；全量测试 765 个通过、0 个失败。

# 本次检查：nlc_2024 漏翻诊断（2026-07-27）

4. 截图中的漏翻已定位：原 PDF 第 54 页（进度键 `53`）被模型原样返回日文，旧校验仍将其记为完成；这是正文漏翻，不是 Word 重排。
5. 修复应新增日文假名残留的硬失败校验，并只重译已定位页、再重新导出；不能只依赖“翻译失败页为 0”。

# 本次检查：nlc_2024 漏翻诊断（2026-07-27）

1. 已核对原 PDF、译文 HTML、进度与质量报告；原 PDF 第 81 页的文字已译出，Word 页码变化源于关闭原页强制分页。
2. 确认原 PDF 第 1 页封面标题及第 41、42、70 页地图/流程图均无文字层；旧流程将其视为插画页而跳过，可见日文没有进入译文。
3. 建议只补这 4 页：封面单独覆写标题；三张图先 OCR 提取并人工校对日文标签，再译为中文并按原图位置覆盖；不重跑其余 110 页。

# 本次更新：同步远端 main（2026-07-27）

1. 已从 `37e184c` 快进至 `86cfb2d`，本地未提交代码已完整恢复。
2. 已完成全量测试：762 个通过、0 个失败。
3. 本地改动的临时备份保留在 Git stash，名称为 `codex-pre-update-2026-07-27`。

# 本次修复：Owlshead Mountain 高保真 HTML（2026-07-23）

- 已统一橙红标题：全部使用第二张截图的 20px、700 字重样式；`Industria-Solid` 不再被误判为常规字重。
- 已修复第 32 页英文叠字，以及第 27、31、32、34 页明显裁切。
- 已统一“枭首山”专名并修正少量明确漏译；未做整册重译。
- 已对照检查 42 页源 PDF；仍有较小超框风险，集中在第 8、10、17、22、24、25、30、36、38、40 页。
- 全量测试 642 个通过、0 个失败；最终 HTML 已离线重建。

# 本次修复：高保真表格导出（2026-07-21）

- 已定位失败页：内部编号 `p0018`（第 19 页）。
- 已修复：同一列的续行文字不再被误识别为新列。
- 已新增覆盖测试；相关测试 7 个通过、0 个失败。
- 已尝试离线重建当前文件，但执行环境暂时无法启动进程；未重新翻译或调用接口。

# 本次修复：启动脚本 Python 路径（2026-07-21）

- 已修复 `start_web.ps1`：接收 Python 命令时固定为数组，避免被拆成 `C:`。
- 已完成静态复核；当前执行环境无法启动 PowerShell，未能运行真实启动验证。

# 当前计划

1. 整理项目现状 -> 对齐功能入口、测试现状、主要流程
2. 检查风险和隐患 -> 找出会误导使用或增加维护成本的问题
3. 收口无用开发内容 -> 先清理明显过期的脚本说明和重复文档

# 本次更新：翻译模型选择（2026-07-28）

## 计划进度

1. 将常用模型改为下拉选择 -> 已完成，DeepSeek 提供 Pro/Flash，Gemini 提供三个 Flash 选项
2. 保留自定义兼容接口 -> 已完成，原有非标准地址和模型不会被改写
3. 按模型载入官方美元单价 -> 已完成，费用与报告统一显示美元；自定义接口默认不估算费用
4. 精简诊断选项 -> 已完成，模糊术语匹配不再出现在 Web 常规界面；校对和提取预览改为明确的诊断说明
5. 运行测试与页面检查 -> 已完成，775 个测试通过；DeepSeek 与 Gemini 切换后单价正确更新

# 本次更新：翻译可靠性与排版切分（2026-07-26）

## 完成记录

- 已补齐翻译截断、术语冲突、断点损坏、规则数值和省略占位符的显式失败处理。
- 已修复文本区域逐行拆分与跨栏句子断裂；旧排版产物需重新运行才会应用新规则。
- README 已同步更新用户可见的行为、报告和限制。
- 全量测试 760 通过、0 失败。

# 本次更新：高保真排版细节（2026-07-28）

## 完成记录

- 定位正文行高改为 1.4，定位标题允许换行；空间不足会由导出检查报错。
- 全大写正文标签未翻译会重试，旧缓存不会复用；标题字体按语义角色统一。
- Web 高保真 HTML 标题使用原始上传文件名；删除了已确认相同的旧 `_typeset_fixed.html`。
- 相关测试 68 通过、0 失败；旧结构产物需重新运行后才能验收新排版。

# 本次修正：工作台控件视觉（2026-07-17）

## 计划进度

1. 定位三处控件异常 -> 已完成，根因是上传按钮样式误作用于删除按钮、多选框内层被重复着色、启动按钮缺少操作间距
2. 修正界面 -> 已完成，上传区只保留“选择文件”，输出框内层透明，启动按钮独立并使用主操作样式
3. 验证 -> 已完成，8501 正常响应；独立预览已检查上传后状态、输出格式与启动区；17 个相关测试通过

## 关键决定

- 只调整 `app.py` 和 `webui/theme.py` 的展示逻辑，不改变翻译流程。

# 本次修正：术语扫描加载提示（2026-07-17）

## 计划进度

1. 定位加载空白 -> 已完成，发生在导入文件后的术语扫描阶段
2. 替换加载反馈 -> 已完成，扫描期间显示 DG 状态卡和旋转指示器，完成后自动移除
3. 清理空白容器 -> 已完成，术语区不再生成空卡片
4. 验证 -> 已完成，真实 PDF 导入时状态卡可见；17 个相关测试通过

## 关键决定

- 加载提示只覆盖术语扫描，不影响首页或翻译执行流程。

# 本次修正：翻译运行态工作台（2026-07-14）

## 计划进度

1. 定位运行后界面回退 -> 已完成，旧档案卡、阶段条和日志仍在逐次追加
2. 改为原位更新 -> 已完成，同类运行组件始终复用一个显示位置
3. 统一运行态视觉 -> 已完成，改为浅色档案卡、分段进度和活动记录
4. 验证 -> 已完成，579 个测试通过，独立端口服务启动正常

## 关键决定

- 不改翻译状态机，只调整运行信息的呈现和更新方式。
- 当前 8501 翻译服务不重启，避免打断正在执行的任务；改动在下次正常重启后生效。
- 长文件名强制换行，运行组件限制在内容宽度内，移除旧黑色终端块和横向溢出。

## 预览状态

- 新版真实项目已在 `http://127.0.0.1:8502/` 独立启动。
- 8501 原翻译服务未改动。

## 本次视觉修正

- 修复浅色主题中多选框和数字按钮的深底深字问题。
- 多选项、加减按钮、帮助图标和滑杆已统一为高对比浅色控件。
- 预览服务已重启，全量 579 个测试通过。

# 本次检查：Holy War 翻译产物（2026-07-14）

## 计划进度

1. 确认原件与最新输出 -> 已完成，原 PDF 共 110 页
2. 检查翻译完整性 -> 已完成，0 个 API 失败，确认第 12、13、62 页有截断
3. 检查输出结构 -> 已完成，97 个非空页均进入 Word 和 HTML
4. 检查图片与格式 -> 已完成，Word/HTML 均未包含原书图片

## 关键结论

- 13 个空页都是整页图片；其中第 2、20、38、41、60、64、69、76 页含封底文字、章节引语或地图信息，当前产物有内容遗漏。
- 第 13 页还遗漏 `1D20/1D100 SAN`；第 81 页属性块保留了大量英文技能名。
- 页 13、17 的“克涅皮尔”和页 56 的“基马瓜”与全书主要译名不一致；弯引号形式的 `Y’golonac` 未被现有术语别名稳定覆盖。
- 当前文件是双栏阅读版，不是原版复刻；未发现 Markdown 标记泄漏，两个表格均已进入 Word/HTML。
- 本次只读检查，没有修改、打开或重新生成用户正在查看的 Word。

# 本次更新：Holy War 术语别名（2026-07-14）

## 计划进度

1. 检查基础表和任务术语表 -> 已完成
2. 新增别名 -> 已完成
3. 验证精确匹配 -> 已完成，22 个术语测试通过

## 新增条目

- `伊戈罗纳克 -> Y’golonac`
- `奇马瓜湖 -> Lake Chimagua`

## 关键决定

- 同时更新基础 `glossary.tsv` 和本次任务使用的 reviewed glossary。
- 保留原有直引号 `Y'golonac`，新增弯引号条目，不使用模糊匹配。

# 本次改动：GREENFILE DG 风格样例

## 计划进度

1. 保留原有工作流 -> 已完成，四个页面与交互路径不变
2. 政府高密级风格化 -> 已完成，加入密级条、机构侧栏、案件编号、权限标签和档案印章
3. 桌面与移动端检查 -> 已完成，无横向溢出，四个主导航均可正常切换

## 关键决定

- 使用“高密级政府内网 + 克制的 DG 档案语言”，不做荧光终端和持续动画。
- 只修改独立网页样例，不接入现有业务前端。

# 本次改动：真实前端 GREENFILE 风格化

## 计划进度

1. 确认真实前端双模式 -> 已完成
2. 重构默认 DG 模式 -> 已完成
3. 桌面与移动端检查 -> 已完成，无横向溢出和页面异常

## 关键决定

- 改动真实 `app.py` 与 `webui/theme.py`；独立样例只作为视觉参考。
- 默认 DG 模式改为高密级政府内网；办公模式和翻译逻辑保持不变。
- 移除启动遮罩、扫描线和持续雷达动效，优先保证工作台可读性。

# 本次改动：真实工作台层级重构

## 计划进度

1. 重构侧栏工作区 -> 已完成，加入四个锚点入口和任务控制区
2. 重构主区层级 -> 已完成，加入顶部栏、四步流程、档案操作区和任务摘要
3. 响应式与真实页面检查 -> 已完成，桌面和手机无横向溢出
4. 验证 -> 已完成，578 个测试通过

## 关键决定

- 不再保留默认 DG 模式的大 Hero 和三张展示卡，首页直接进入任务工作流。
- 当前保持 Streamlit 单页业务模型，侧栏入口定位到执行、校对和档案区域，不改翻译状态机。
- 办公模式继续使用原结构，避免这次视觉重构扩大到无关范围。

# 本次修正：移除旧式侧栏表单和启动区

## 计划进度

1. 清空侧栏任务参数 -> 已完成，侧栏输入控件为 0
2. 参数移入主流程 -> 已完成，集中到“第二步 · 范围与输出”
3. 重做预检与启动 -> 已完成，旧启动组件为 0
4. 验证 -> 已完成，桌面和手机无横向溢出，578 个测试通过

## 关键决定

- 侧栏只保留工作区导航、DeepSeek 链路状态和办公界面开关。
- 顶部四步流程改为可点击锚点，分别定位到导入、设置、术语和启动区域。
- 启动区使用白色预检面板、三项摘要、风险提示和右下主按钮，不再使用深色状态条。

# 当前进度

- 已确认主线功能：PDF / Markdown / Word 输入，输出 HTML / Word / Markdown，另有独立的 `_typeset.pdf` 流程
- 已确认测试现状：`.venv` 下 `pytest` 487 通过；系统 Python 直接跑 `pytest` 失败，说明实际工作流依赖项目虚拟环境
- 已完成整理：`test_setup.py` 改为检查 Streamlit；`docs/GUIDE.md` 改成精简版；`README.md` 的开发自检命令改为使用 `.venv`
- 已完成修复：`start_web.ps1` 不再在启动时强制下载 Chromium；Web 端导出 `_typeset.pdf` 时会按需提示加载浏览器内核插件并显示进度
- 已完成删减：移除 `.kiro` 开发配置、根目录历史计划、`docs/plans/*`、旧设计/路线图/任务/测试说明；清理本地 `dist/`、`scratch/` 和缓存目录

# 关键决定

- 先不碰主翻译逻辑，只修会误导使用和维护的旧内容
- `README.md` 作为唯一完整使用说明，`docs/GUIDE.md` 只保留最短入口
- 输出目录、打包产物、实验样本先只标记为可清理对象，不直接删除用户数据
- 浏览器内核改为按需安装：普通 Web 启动不依赖 Chromium，只有纯重绘 PDF 流程才触发加载
- 删除原则：保留运行入口、测试、用户说明、术语表和真实输出；删除无引用的开发过程材料和本地临时产物

# 本次检查：The_Borellus_Connection_linked (1).pdf

## 计划进度

1. 定位 PDF 和项目转换入口 -> 已完成
2. 检查 PDF 文本层和页面类型 -> 已完成
3. 判断 Word/HTML 兼容性 -> 已完成

## 结论

- PDF 共 421 页，文字层存在；不是纯扫描件。
- 几乎每页都有整页图片层，普通 Word/HTML 路线会忽略图片层，主要风险不在图片。
- 当前普通 Word/HTML 路线不能直接放心跑整本：目录识别过宽，13 页被当作目录页，其中多页正文会被截短。

## 关键决定

- 暂不改代码，只给兼容性结论。
- 后续若要支持这本 PDF，优先修 `core/extractor.py` 的目录识别和目录抽取，确保不漏正文。

# 本次改动：目录误判修复与三栏输出

## 计划进度

1. 收紧目录识别 -> 已完成，误判目录页从 13 页降到 4 页
2. 支持三栏阅读布局 -> 已完成，三栏页会标记为 `three_columns`
3. Word 分栏选项扩展 -> 已完成，可选单栏、双栏、三栏
4. 验证 -> 已完成，`.venv` 下 496 个测试全部通过

## 关键决定

- 普通 HTML/Word 走阅读版三栏复原，不追求还原整页背景图和贴纸层。
- 真正的整页视觉复原仍应走 `_typeset.pdf` 独立流程。
- 不用兜底猜测：目录识别只认明确 Contents/Index 或大量点线页码。

# 本次检查：卡片类内容识别

## 计划进度

1. 统计全书结构标记 -> 已完成
2. 抽查截图页 023 / 031 -> 已完成
3. 判断兼容性 -> 已完成

## 结论

- 当前能识别一部分卡片：全书识别出 136 个 `[CARD]`，覆盖 105 页。
- 当前不能完整识别属性小卡：`[STAT_BLOCK]` 为 0。
- 截图页 023 只识别了顶部黄色纸条，灰色属性块和白色刷底说明块多为普通正文。
- 截图页 031 识别了底部 `SALT-CUT INVESTIGATORS` 卡片。

## 关键决定

- 普通翻译不会因此漏文字，但卡片语义分类还不够完整。
- 若要更像原版排版，需要新增属性块、窄栏说明块、纸条块的几何识别规则。

# 本次改动：FoDG 属性卡识别

## 计划进度

1. 定位样本页 -> 已完成，原书页 040 对应 PDF 第 45 页
2. 新增属性卡分组 -> 已完成，支持 Courier 属性卡和 Futura 属性卡
3. 防止误伤技能表 -> 已完成，纯技能名/数值列表不会被当成属性卡
4. 验证 -> 已完成，`.venv` 下 499 个测试全部通过

## 结论

- 第 45 页现在能识别出 2 个 `[STAT_BLOCK]`：`MING YUAN` 和 `Large Poppet`。
- 全书当前识别出 64 个 `[STAT_BLOCK]`，覆盖 54 页。
- 普通 `[CARD]` 当前为 126 个。

## 关键决定

- 属性卡必须有明确标签，例如 `Abilities:`、`General Abilities:`、`Hit Threshold:`、`Attack:`、`Armor:`。
- 只靠 `Drive 5`、`Firearms 12` 这类技能表数字不判定为属性卡。

# 本次检查：30 页输出表格误判

## 计划进度

1. 检查输出审计 -> 已完成，1-30 页输出中第 7 页失败，实际完成 26 页
2. 定位表格误判 -> 已完成，第 23-26 页有等宽正文被误判成表格
3. 收紧表格识别 -> 已完成，正文里的字体切换、断词、普通 `name` 不再触发表格
4. 验证 -> 已完成，`.venv` 下 501 个测试全部通过

## 结论

- 原输出确实有问题：部分普通段落被包成 Markdown 表格，Word/HTML 会被拆开。
- 当前源码重新抽取前 30 页，已没有误包出来的管道表格行。
- 第 7 页是独立翻译失败，不是排版识别问题。

## 关键决定

- 表格识别只接受强信号：管道符、分隔线、明确表头，或有明显空隙并稳定对齐的三列网格。
- FoDG 的普通等宽说明文字不再按表格处理。

# 本次检查：完整 Word/HTML 输出

## 计划进度

1. 检查完整输出审计 -> 已完成，421 页中完成 396 页，7 页翻译失败
2. 扫描 HTML/Word 标记泄漏 -> 已完成，发现目录围栏 ` ```toc ` 泄漏
3. 扫描表格误判 -> 已完成，发现第 102、150 页等正文被旧规则拆成表格
4. 修复规则并验证 -> 已完成，`.venv` 下 504 个测试全部通过

## 结论

- 当前已生成的完整 Word/HTML 不是最终可放心版：缺 7 页，且保留了生成时的目录围栏和表格误拆问题。
- 当前源码已修复：全书重新提取后没有正文误拆成 Markdown 表格。
- 还有少量英文卡片标题残留，例如 `Scene Type`、`Lead-Ins`、`Lead-Outs`、`Local Contacts`。

## 关键决定

- `10+ hours`、`4+ Health damage` 这类普通加号不再触发表格识别。
- HTML/Word 导出会自动修正挤在同一行的 ` ```toc ` 目录围栏。
- 现有输出文件需要重新生成才会吃到本次修复。

# 本次修复：失败页与重翻页同时执行

## 计划进度

1. 检查界面执行状态 -> 已完成，实际生成了带后缀的新输出
2. 定位未重翻原因 -> 已完成，`只重试失败页` 覆盖了 `重翻页码`
3. 修复任务页选择 -> 已完成，失败页与重翻页会合并执行
4. 验证 -> 已完成，`.venv` 下 504 个测试全部通过

## 结论

- 之前同时勾选 `只重试失败页` 并填写 `102,150` 时，102 和 150 会被清掉旧译文但没有进入翻译队列。
- 现在会一起处理失败页和手动重翻页。

## 关键决定

- 重翻页码优先级不能被 `只重试失败页` 吃掉。

# 本次新增：Workspace 全局工作台

## 计划进度

1. 检查现有规范文件 -> 已完成
2. 创建全局工作台文档 -> 已完成
3. 创建复利与踩坑日志 -> 已完成
4. 创建新项目 SOP -> 已完成
5. 接入 `AGENTS.md` -> 已完成

## 结论

- 已创建 `Workspace/00_GLOBAL_WORKBENCH.md`，作为总入口。
- 已创建 `Workspace/01_COMPOUND_LESSONS_LOG.md`，用于沉淀错误和复用方法。
- 已创建 `Workspace/02_NEW_PROJECT_SOP.md`，规定新项目目录和命名。
- 已创建 `Workspace/projects/README.md`，作为项目容器说明。

## 关键决定

- 新项目统一放在 `Workspace/projects/projectNNN_项目名称/`。
- 项目资料、过程文件、输出和日志都必须放入对应项目文件夹。
- `AGENTS.md` 已加入全局工作台入口规则。

# 本次优化：DeepSeek 缓存命中

## 计划进度

1. 分析后台 token 结构 -> 已完成，缓存命中率约 37.4%
2. 降低默认并发 -> 已完成，默认并发从 32 改为 8
3. 固定 system prompt -> 已完成，页内术语改放 user prompt 末尾
4. 验证 -> 已完成，`.venv` 下 506 个测试全部通过

## 结论

- 之前每页术语会进入 system prompt，导致 system prompt 每页变化，不利于 DeepSeek 前缀缓存。
- 现在 system prompt 固定，页内术语仍会发送，但不再破坏 system prompt 缓存。
- 默认并发降低后，更利于缓存先写入再被后续请求命中，也能减少 API 报错。

## 关键决定

- 缓存友好优先：稳定规则放 system，变化内容放 user。
- 默认并发使用 8；需要速度时可手动调高。

## 后续计划

1. 缓存预热 -> 先用 1 个请求翻译首页或首个待翻页，完成后再启动并发，验证 DeepSeek 缓存命中率是否提升。
2. 固定核心术语表 -> 从 `glossary.tsv` 中抽取 50-100 个高频术语，作为稳定前缀发送，验证命中率和术语一致性。

# 本次优化：DeepSeek 缓存预热与核心术语前缀

## 计划进度

1. 缓存预热 -> 已完成，PDF 并发前先翻译首个真实待处理页
2. 固定核心术语表 -> 已完成，从本次源文本命中的术语中选最多 80 个高频项，放到 user prompt 开头
3. 缓存隔离 -> 已完成，`PROMPT_VERSION` 升级到 v14，避免旧进度缓存混用
4. 验证 -> 已完成，`.venv` 下 510 个测试全部通过

## 结论

- system prompt 继续固定，不再塞入每页变化的术语。
- user prompt 开头现在有稳定核心术语前缀；页内补充术语仍放末尾，重复项会去掉。
- PDF 并发前会先预热 1 页，再启动并发，给 DeepSeek 前缀缓存先写入机会。

## 关键决定

- 核心术语只从本次实际源文本命中的术语里选，不直接塞完整术语表。
- 提示词结构变化必须提高 `PROMPT_VERSION`，让旧译文进度明确失效。

# 本次调研：GitHub 同类项目优化灵感

## 计划进度

1. 查找同类项目 -> 已完成，重点看 PDF 翻译、文档翻译、PDF 解析三类
2. 提炼可借鉴方向 -> 已完成，优先选择不破坏当前主线的优化点

## 结论

- 最接近的是 PDFMathTranslate、BabelDOC、DocuTranslate、LLM_PDF_Translator。
- 最值得借鉴的是：可视化质量检查、术语候选确认、任务级 manifest 校验、解析后端显式切换。
- 不建议直接追求完整 PDF 原版复刻；这会明显扩大复杂度，应继续保留阅读版和 `_typeset.pdf` 两条路线。

## 关键决定

- 新优化应优先围绕“减少失败返工”和“让用户更容易判断结果可靠”。
- 外部解析器只能作为显式高级模式，不做静默兜底。

# 本次新增：质量检查台

## 计划进度

1. 新增质量检查逻辑 -> 已完成，统一检查失败页、缺译文、疑似未翻译、疑似截断、英文残留和术语遗漏
2. 接入 Web PDF 流程 -> 已完成，翻译结束后直接显示质量检查汇总和问题页
3. 接入 CLI PDF 流程 -> 已完成，生成 `_quality_report.md`
4. 验证 -> 已完成，`.venv` 下 513 个测试全部通过

## 结论

- Web 端现在会显示检查页数、有译文页数、失败页、待检查问题和术语遗漏。
- 每次 PDF 翻译会生成质量检查报告，供后续复查。
- `_quality_report.md` 已归类为内部报告，不再被档案库当成最终成品。

## 关键决定

- 疑似未翻译和疑似截断仍按现有规则拦截。
- 英文残留和术语遗漏只作为待校对提示，不直接阻止导出。
- 质量检查里的术语命中使用翻译时同一个匹配器，避免检查结果和实际 prompt 不一致。

# 本次新增：术语候选确认

## 计划进度

1. 汇总疑似未收录专名 -> 已完成，按出现页数和次数排序
2. 接入 Web PDF 流程 -> 已完成，翻译结束后显示前 30 个候选术语
3. 生成候选文件 -> 已完成，输出 `_glossary_candidates.md` 和 `_glossary_candidates.tsv`
4. 验证 -> 已完成，`.venv` 下 515 个测试全部通过

## 结论

- Web 端现在能看到高频疑似专名候选。
- 候选 TSV 的中文列默认留空，直接加载会被跳过，不会污染术语表。
- 候选报告已归类为内部报告，不会被档案库当成最终成品。

## 关键决定

- 只做候选提示，不自动写入 `glossary.tsv`。
- 候选来自提取后的源文，仍需人工确认后再加入术语表。

# 本次新增：直观校对与运行报告

## 计划进度

1. 问题页对照校对 -> 已完成，质量检查的问题页可直接查看英文原文和中文译文片段
2. 运行清单 -> 已完成，输出 `_manifest.json`，记录输入、设置、结果和报告文件
3. 效果报告 -> 已完成，输出 `_run_report.md`，Web 端也显示缓存命中率、API 调用和平均每页费用
4. 验证 -> 已完成，`.venv` 下 518 个测试全部通过，并编译检查 `app.py`

## 结论

- 问题页不再只是页码列表，能直接在界面里对照校对。
- 每次 PDF 翻译会额外生成运行清单和效果报告。
- `_manifest.json` 和 `_run_report.md` 已归类为内部报告，不会被档案库当成最终成品。

## 关键决定

- 保留方便直观的信息，不继续加入高级解析、术语在线编辑等复杂功能。
- 效果报告只展示能帮助判断本次运行是否顺利的数字。

# 本次优化：项目体积清理

## 计划进度

1. 定位空间占用 -> 已完成，主要来自 `.venv`、`uploads`、`.git`、`output`
2. 上传去重 -> 已完成，新上传文件按 SHA256 复用，避免同一文件重复保存
3. 清理本地体积 -> 已完成，删除重复上传、旧 progress、Python 缓存，重建 `.venv`，压缩 Git 对象
4. 验证 -> 已完成，`.venv` 下 519 个测试全部通过

## 结论

- 项目目录从约 1.35GB 降到约 732MB。
- `uploads` 从约 490MB 降到约 126MB。
- `.git` 从约 184MB 降到约 0.8MB。
- `.venv` 从约 562MB 降到约 461MB。

## 关键决定

- 保留 `output/` 成品，不删除历史输出。
- 只删除重复上传、旧 progress 和可再生成缓存。
- `logs/` 已加入 `.gitignore`。

# 本次检查：收尾审查

## 计划进度

1. 通读入口、配置、文档和启动脚本 -> 已完成
2. 检查重复逻辑、未使用内容、README 一致性、误操作、密钥和 Windows 启动风险 -> 已完成
3. 验证当前状态 -> 已完成，`.venv` 下 519 个测试全部通过

## 结论

- 发现真实密钥风险：`config.json` 未加入 `.gitignore`，但 README 建议用户把 API Key 写入该文件。
- 发现配置误导：`config.example.json` 中的 `rate_limit`、`cooldown`、`max_split_depth` 对 PDF 命令行入口不生效。
- 发现并发设置不一致：Web 默认 8，CLI 和示例仍是 32。
- 发现文档过期：README 里的 `docs/` 描述仍写设计、路线图、计划和测试说明，实际只剩精简指南。
- 发现可清理代码：存在重复失败判断、重复文件替换重试、重复图片/表格/目录解析 helper、旧 sequential typeset 翻译函数和部分未使用 helper。
- 发现 Windows 启动风险：启动脚本不检查 Python 版本，`py -3` 可能选到低版本；`test_setup.py` 仍按 Python 3.9+ 判断。

## 关键决定

- 本轮只做检查和记录，不改业务代码。
- 后续如要清理，优先处理密钥风险、配置误导和 Windows 版本检查，再处理重复/废弃代码。

# 本次修复：收尾问题闭环

## 计划进度

1. 修复密钥与配置风险 -> 已完成，`config.json` 已忽略，PDF CLI 支持 `rate_limit` 和 `cooldown`
2. 修复 Windows 启动检查 -> 已完成，启动脚本和环境检查统一要求 Python 3.10+
3. 清理明显旧代码 -> 已完成，移除未引用的旧函数和重复 helper
4. 验证 -> 已完成，`.venv` 下 522 个测试全部通过，环境检查通过

## 结论

- README、配置模板和 PDF CLI 默认并发已统一为 8。
- PDF 配置文件遇到不支持字段会直接失败，不再静默忽略。
- 纯检查发现的高风险项已处理：API Key 文件、配置误导、Windows 版本坑。
- 清理了旧 typeset 顺序翻译函数、旧 Word 块合并函数、旧 Markdown 拆分函数，以及重复失败判断和重复文件替换逻辑。

## 关键决定

- `max_split_depth` 只保留给 Markdown / Word 失败拆分流程，PDF 配置不再接受该字段。
- `translate_pdf.py` 的兼容再导出层先保留，避免破坏旧脚本引用。

# 本次文档：常见失败案例

## 计划进度

1. 确认 README 插入位置 -> 已完成，放在质量检查建议后、常见问题前
2. 增加失败案例处理表 -> 已完成，覆盖 8 类常见失败
3. 验证 -> 已完成，8 个条目均已写入 README，文档空白检查通过

## 结论

- README 新增“常见失败案例与处理方式”章节。
- 内容面向普通 Windows 用户，按失败表现和处理方式快速定位。

## 关键决定

- 用表格承载，避免把常见问题章节继续拉长。

# 本次修复：质量检查问题页可直接重翻

## 计划进度

1. 接通 Web 质量检查选择页 -> 已完成，问题页可勾选并一键写入重翻页码后自动执行
2. 复用现有 retry/progress -> 已完成，仍走原来的进度文件、页码解析、翻译进度条
3. 同步 CLI 行为 -> 已完成，CLI 支持 `--retranslate-pages`，配置文件支持 `retranslate_pages`
4. 验证 -> 已完成，相关测试 71 个通过，语法检查通过

## 关键决定

- 重翻必须清掉该页旧译文对应的 prompt 缓存，避免看似重翻但实际复用旧结果。
- 不新增兜底流程；质量检查按钮只负责选择页码，实际翻译仍交给现有进度和 retry 逻辑。

# 本次检查：Delta Green Meridian Word/HTML 校对

## 计划进度

1. 确认输出范围 -> 已完成，源 PDF 34 页，本次翻译 1-31 页，32-34 页为空白预设卡模板
2. 检查 Word/HTML 本体 -> 已完成，译文主体正常，无整页未翻或坏字符
3. 定位不舒服处 -> 已完成，主要是专名与状态名不一致

## 关键决定

- 不需要整本重翻；优先修 Holy Light Ministries、The Sheltered、Seen、Awakened、Task Force 等名称一致性。

# 本次讨论：在线部署方式

## 计划进度

1. 检查启动入口 -> 已完成，Web 使用 Streamlit，默认端口 8501
2. 检查依赖 -> 已完成，Python 3.10+，依赖见 requirements.txt
3. 部署建议 -> 已完成，优先私有访问，不建议裸露公网

## 关键决定

- 当前项目没有登录鉴权；部署到公网前应放在 VPN、反向代理认证或内网访问后面。

# 本次修复：PDF 小标题加粗识别

## 计划进度

1. 定位漏加粗原因 -> 已完成，截图里的小标题在提取阶段没有被标成标题
2. 修复标题识别 -> 已完成，支持按字体名识别粗体，并放宽粗体小标题比例
3. 验证实际页面 -> 已完成，Meridian 对应页已能抽出四级小标题
4. 验证测试 -> 已完成，相关测试 92 个通过，语法检查通过

## 关键决定

- 修在 PDF 提取层，不在 Word/HTML 导出层补样式；旧缓存会因为提取版本更新而失效，避免继续复用错误结果。

# 本次检查：项目健壮性与 TRPG 扩展

## 计划进度

1. 识别项目范围 -> 已完成，项目是本地 TRPG 文档翻译工具
2. 本地测试验证 -> 已完成，525 个测试通过，语法检查通过，环境检查通过
3. 对照同类 GitHub 项目 -> 已完成
4. 推演功能风险和 TRPG 扩展方向 -> 已完成

## 关键决定

- 本轮只做检查、对比和建议，不改业务代码。
- 以 PDFMathTranslate、BabelDOC、Marker、bilingual_book_maker、Zotero PDF Translate 等公开项目作横向参照。
- 结论：当前代码对个人本地 TRPG 翻译足够稳，但还不是通用文档翻译平台。
- 主要风险：进度文件被外部破坏时可能误判完成页；API 永久失败会保留进度并继续，适合长任务但不够“立刻失败”；纯重绘 PDF 和复杂版面仍是高风险功能。
- TRPG 扩展优先方向：术语库管理、怪物/技能/道具结构化抽取、模组校对清单、骰子与规则符号检查、Foundry/Roll20 导出。

# 本次检查：Unspeakable Oath Word/HTML 阅读体验

## 计划进度

1. 定位最新输出和源文件 -> 已完成，输出为 The_Unspeakable_Oath_16_17-114-128_cn，源 PDF 15 页
2. 对照源 PDF、HTML、Word -> 已完成，重点看第 1、2、4、9、13、14 页
3. 记录后续 TRPG 功能 -> 已完成

## 关键决定

- 纯重绘成本高，后续优先优化 Word/HTML 阅读体验。
- 当前成品可读，但第 9 页失败缺失，HTML/Word 没有明显占位。
- 源第 1 页是封面图，当前成品生成了空白感很强的标题页。
- 资料卡和 NPC 属性块应优先做成更易扫读的卡片或表格式结构。
- 后续功能先进入待办：术语库管理、规则符号检查、模组校对清单、结构化抽取、Foundry/Roll20 导出。

# 本次修复：Word/HTML 阅读体验第一轮

## 计划进度

1. 修复标题误判 -> 已完成，正文句子、坏 OCR 页眉、属性行不会再当标题
2. 简洁封面 -> 已完成，HTML 封面改为紧凑阅读版，Word 封面减少空白
3. 资料卡优化 -> 已完成，目标档案人物条目可渲染为卡片
4. 坏 OCR 标题清理 -> 已完成，正文和 running header 共用同一套标题清理
5. 验证 -> 已完成，532 个测试通过，语法检查通过，已离线重排 Unspeakable Oath 的 HTML/Word

## 关键决定

- 不修第 9 页缺页占位，由用户先检查失败页。
- NPC 属性块本轮先修“误判标题”，已有卡片/属性块标记的内容继续走卡片样式；未标记的旧译文不强行重构。
- 术语误伤方案：术语表后续分为“专名强制、规则术语、弱提示、禁用”四类；规则术语只在属性块、技能表、规则段落中强制，不在普通叙事句里强制替换。
# 本次开发：翻译前术语确认

## 计划进度

1. 找到 Web 上传与术语表链路 -> 已完成，原流程是上传后直接翻译，候选词只在翻译后报告
2. 增加翻译前术语确认 -> 已完成，上传文件后网页会显示候选术语，可选择新增、修改或忽略
3. 接入本次翻译 -> 已完成，只有选择新增/修改时才生成临时术语表，并让本次翻译使用它
4. 验证 -> 已完成，全量 538 个测试通过，语法检查通过

## 关键决定

- 不改原 glossary.tsv，用户确认后的术语只写入本次输出目录里的临时 TSV。
- 新增不能覆盖已有词，修改必须针对已有词；填错直接报错，不做猜测处理。
- PDF、Markdown、Word 都接入同一套术语确认逻辑。
# 本次开发：备团校对清单与结构化抽取

## 计划进度

1. 确认接入点 -> 已完成，先接 PDF 翻译完成后的报告阶段
2. 生成备团清单 -> 已完成，合并质量问题、术语候选、数据块、卡片和规则提示
3. 生成结构化资料 -> 已完成，输出 Markdown 与 JSON，便于后续做检索、导出和备团整理
4. 接入网页 -> 已完成，翻译完成后显示待处理、数据块、卡片、规则提示数量，并加入下载列表
5. 验证 -> 已完成，全量 543 个测试通过，语法检查通过

## 关键决定

- 本轮只接 PDF 流程，因为 PDF 流程有可靠页码；Markdown/Word 暂不做假页码。
- 结构化抽取先使用明确标记和规则符号，不调用模型，不猜剧情关系。
- 输出文件为 `_prep_checklist.md`、`_module_structure.md`、`_module_structure.json`。
# 本次开发：失败页/风险页工作台

## 计划进度

1. 确认数据来源 -> 已完成，复用质量报告和提取诊断，不新增猜测算法
2. 合并风险条目 -> 已完成，集中展示翻译失败、无译文、疑似未翻页、截断、英文残留、术语冲突、OCR/提取风险
3. 接入网页操作 -> 已完成，可选择页码后重翻，或标记忽略、清除忽略
4. 输出报告 -> 已完成，生成 `_risk_workbench.md`
5. 验证 -> 已完成，全量 548 个测试通过，语法检查通过

## 关键决定

- 只接 PDF 流程，因为风险页和页码来自 PDF 提取诊断。
- “未提取到正文”的页不默认作为可重翻页，避免对纯图片页无效重翻。
- 工作台不替代质量报告；它把质量报告和提取诊断集中成一个处理入口。
# 本次开发：HTML 阅读模式切换

## 计划进度

1. 查看 HTML 导出结构 -> 已完成，现有成品是固定纸张分页样式
2. 增加模式切换 -> 已完成，HTML 顶部新增屏幕版、打印版、手机版按钮
3. 优化手机阅读 -> 已完成，手机版强制单栏、减少边距、取消正文缩进
4. 保持打印可用 -> 已完成，打印时隐藏工具条，保留 Letter 纸张规则
5. 验证 -> 已完成，全量 550 个测试通过，已用 Playwright 生成桌面和手机截图检查

## 关键决定

- 只改普通 HTML 导出，不影响纯重绘 PDF。
- 默认仍是屏幕版，避免改变旧 HTML 打开后的第一眼效果。
- 模式选择写入浏览器本地存储，同一浏览器下次打开会记住选择。
# 本次开发：规则符号检查

## 计划进度

1. 确认保留规则 -> 已完成，骰子、SAN/HP/WP、属性缩写、技能检定本来就要求模型保留
2. 增加翻译后检查 -> 已完成，检查骰子、损失格式、属性/数值缩写是否漏保留或被翻成中文
3. 接入质量报告 -> 已完成，规则符号疑点会进入质量检查和失败页/风险页工作台
4. 输出报告 -> 已完成，生成 `_rule_symbols.md`
5. 验证 -> 已完成，全量 557 个测试通过，语法检查通过

## 关键决定

- 不新增猜测式修复，只报告疑点，交给重翻或人工确认。
- `1D6` 和 `1d6` 视为同一骰子格式，避免大小写误报。
- 技能英文残留只提示人工确认，不直接判定为错误。
# 本次开发：Word 校对包与场景时间线

## 计划进度

1. 确认接入点 -> 已完成，继续接 PDF 翻译完成后的报告阶段
2. 场景时间线抽取 -> 已完成，识别 D-1、Day 1、星期、时间点、午夜/黎明/第二天等明确标记
3. Word 校对包 -> 已完成，汇总问题页索引、术语疑点、规则符号疑点和时间线
4. 输出文件 -> 已完成，生成 `_timeline.md`、`_timeline.json`、`_word_review.md`，有 python-docx 时生成 `_word_review.docx`
5. 验证 -> 已完成，全量 563 个测试通过，语法检查通过

## 关键决定

- 不在主 Word 成品里直接插入真实批注，避免破坏现有排版；先生成独立校对包。
- 时间线只抓明确时间标记，不推断隐含先后关系。
- Word 校对包复用已有质量报告、术语候选、规则符号检查和时间线，不重复造判断逻辑。

# 本次文档：README 分享版更新

## 计划进度

1. 对齐当前功能 -> 已完成，补入术语确认、风险工作台、规则符号检查、备团清单、时间线和 Word 校对包
2. 精简分享说明 -> 已完成，保留最快启动、Web 流程、输出文件、术语表、常见问题和打包分享
3. 验证 -> 已完成，README 关键章节和关键报告名检查通过

## 关键决定

- README 改成给新用户看的分享版，不再堆叠过多内部实现细节。
- 默认推荐 HTML 和 Word；纯重绘 PDF 明确标为更慢、更挑结构的高级功能。
- 提醒不要把 `config.json`、`.env`、输出译文或 API Key 一起发出去。

# 本次开发：Web 办公模式

## 计划进度

1. 定位外观入口 -> 已完成，主入口是 `app.py`，样式在 `webui/theme.py`
2. 增加办公模式开关 -> 已完成，默认保留原三角洲界面，开启后切换中性标题、文案和浅色样式
3. 同步状态卡和历史输出 -> 已完成，办公模式下使用文件、任务号、历史输出等中性说法
4. 验证 -> 已完成，21 个相关测试通过，语法检查通过，已检查桌面和手机截图

## 关键决定

- 不做自动识别办公环境，只给用户主动开关。
- 办公模式自动关闭主要动画和强主题视觉。
- 上传后的信息卡也跟随切换，避免继续显示绝密、CLASSIFIED 等字样。

# 本次检查：Web 办公模式效果复查

## 计划进度

1. 启动本地页面 -> 已完成，端口 8517 正常打开
2. 检查桌面效果 -> 已完成，默认模式保留原风格，办公模式显示中性界面
3. 检查手机效果 -> 已完成，没有文字竖排、重叠或明显溢出

## 关键决定

- 本次只做效果检查，不改代码。
- 办公模式当前可用，没有发现需要立刻修的问题。

# 本次修复：原模式控件白底

## 计划进度

1. 定位白底控件 -> 已完成，问题在多选框、上传框、数字输入按钮的 Streamlit 内部样式
2. 修复原模式样式 -> 已完成，补回黑绿终端风格
3. 验证 -> 已完成，12 个相关测试通过，语法检查通过，桌面截图复查通过

## 关键决定

- 只修原模式控件样式，不改办公模式。
- 不做兜底判断，直接按明确的 Streamlit 控件结构补样式。

# 本次修复：办公模式控件串色

## 计划进度

1. 定位串色原因 -> 已完成，原模式黑绿控件样式在切换后压住了办公模式
2. 隔离办公模式控件 -> 已完成，给办公模式补同等强度的浅色规则
3. 验证 -> 已完成，12 个相关测试通过，语法检查通过，原模式和办公模式截图复查通过

## 关键决定

- 两个模式分别维护控件颜色，不再共用上传框、多选框、数字输入按钮的视觉规则。
- 办公模式保持白底浅色，原模式保持黑绿终端风。

# 本次改动：输出校对区收起与中间报告回退

## 计划进度

1. 缩短网页完成页 -> 已完成，翻译后校对区改为高级开关，默认关闭。
2. 回退时间线和备团输出 -> 已完成，普通翻译不再生成时间线、备团清单和模组结构文件。
3. 修复第一页标题 -> 已完成，导出封面按正文顺序取第一个有效标题。
4. 检查明显正文瑕疵 -> 已完成，数据块标题不再露出 `####`。
5. 验证 -> 已完成，`.venv` 中 571 个测试通过。

## 关键决定

- 校对包、风险页工作台、规则符号报告只在打开“显示翻译后校对区”时生成。
- 旧的校对、时间线、备团类文件统一视为内部报告，不再算作正式输出。
- 检查版输出放在原输出目录的 `_rerender_check` 子目录，不覆盖用户正在查看的文件。

# 本次修复：超长上传文件名

## 计划进度

1. 定位术语扫描找不到文件 -> 已完成，原因是上传文件名过长导致保存路径超过 Windows 限制。
2. 修复上传保存名 -> 已完成，按实际目录动态截短文件名前缀，保留哈希防止重名。
3. 验证 -> 已完成，`.venv` 中 573 个测试通过。

## 关键决定

- 文件名只保留可读前缀，唯一性继续靠 SHA256 哈希。
- 上传目录过深到无法安全保存时直接报错，不做静默兜底。
# 本次操作：拉取项目文件

## 计划进度

1. 确认仓库默认分支 -> 已完成，默认分支为 `main`
2. 下载并解压项目文件 -> 已完成，文件已放入当前目录
3. 检查结果 -> 已完成，当前目录已有项目源码和说明文件

## 关键决定

- 本机没有可用的 `git` 命令，改用 GitHub 官方压缩包下载公开仓库。

# 本次操作：检查并打开项目

## 计划进度

1. 检查启动入口和依赖 -> 已完成，入口为 Web 界面
2. 准备本地运行环境 -> 已完成，使用 `uv` 创建 `.venv`
3. 运行项目测试 -> 已完成，573 个测试全部通过
4. 启动并打开网页 -> 已完成，地址为 `http://localhost:8501`

## 关键决定

- 系统 `python` 和 `py` 不可用，使用本机已有的 `uv` 准备项目环境。

# 本次操作：移动项目到 D 盘

## 计划进度

1. 确认 D 盘和目标目录 -> 已完成，`D:\DG-trans` 可用
2. 停止正在运行的 Web 服务 -> 已完成
3. 移动项目目录 -> 已完成
4. 检查移动结果 -> 已完成，项目文件已在 `D:\DG-trans`
5. 检查运行环境 -> 已完成，移动后的 `.venv` 可用
6. 从新位置重新打开 Web -> 已完成，`http://localhost:8501` 正常访问

## 关键决定

- 目标目录使用 `D:\DG-trans`。
- 旧位置只剩空目录，被当前会话占用，暂时无法删除。

# 本次操作：安装 Git 和 SVN

## 计划进度

1. 检查本机命令 -> 已完成，原先没有可用的 `git` 和 `svn`
2. 下载官方安装包 -> 已完成
3. 安装 Git -> 已完成，版本 `2.55.0.windows.2`
4. 安装 SVN -> 已完成，版本 `1.14.5-SlikSvn`
5. 验证命令可用 -> 已完成

## 关键决定

- Git 安装到用户程序目录。
- SVN 使用 SlikSVN 命令行客户端，解包到用户程序目录。

# 本次调研：Rainman Translate Book

## 计划进度

1. 在 GitHub 定位项目 -> 已完成，主仓库为 `deusyu/translate-book`
2. 临时下载并检查源码 -> 已完成，未放入本项目目录
3. 对照当前项目能力 -> 已完成

## 关键决定

- 不直接合并整个项目；它是 Claude Code Skill，依赖 Calibre、Pandoc 和 subagent 流程。
- 可借鉴方向：chunk 级 manifest 校验、术语变化后的精确重译、相邻 chunk 只读上下文、子任务输出 meta 后再合并术语。

# 本次开发前检查：Rainman 功能迁移风险

## 计划进度

1. 检查当前断点和缓存 -> 已完成
2. 检查 PDF / Markdown / Word 三条翻译入口 -> 已完成
3. 检查 Web 重翻和失败重试入口 -> 已完成
4. 梳理风险点 -> 已完成

## 关键决定

- 先不动代码，先确认风险和开发顺序。
- 第一阶段优先做相邻上下文和合并前完整性校验。
- 术语变化精确重译要单独设计，避免破坏现有 `progress.json`。
- 子任务 meta 术语反馈放到后续阶段，不和第一阶段混做。

# 本次开发：相邻上下文与导出前完整性校验

## 计划进度

1. 确认 D 盘项目 -> 已完成，后续以 `D:\DG-trans` 为准
2. 增加相邻上下文 -> 已完成，PDF 翻译会带前一页和后一页只读参考文本
3. 增加导出前完整性校验 -> 已完成，重复页、越界页、漏页会直接报错
4. 验证 -> 已完成，`.venv` 中 578 个测试通过，语法检查通过

## 关键决定

- 失败页仍允许生成部分结果，方便后续重试。
- 有正文但既没有译文也没有失败记录的页，视为进度异常，导出前直接停止。
- 提示词结构已变化，`PROMPT_VERSION` 升级到 v15，避免旧缓存混用。
- 新开窗口不能自动继承本对话，但读取 D 盘项目和本文件后可以接续。

# 本次检查：Git 可用性

## 计划进度

1. 检查 Git 命令 -> 已完成，当前窗口 PATH 没包含 Git
2. 定位 Git 安装路径 -> 已完成，位于 `C:\Users\cytd\AppData\Local\Programs\Git\cmd\git.exe`
3. 检查项目仓库状态 -> 已完成，`D:\DG-trans` 当前不是 Git 仓库

## 关键决定

- 重开 Codex 后通常能继承用户 PATH。
- 若要在当前项目使用版本管理，需要对 `D:\DG-trans` 执行 `git init`，或重新用 `git clone` 获取项目。

# 本次操作：Git 初始化与提交

## 计划进度

1. 检查 Git 命令 -> 已完成，Git 版本 `2.55.0.windows.2`
2. 初始化仓库 -> 已完成，`D:\DG-trans` 已成为 Git 仓库，分支为 `main`
3. 设置可提交状态 -> 已完成，用户提交身份已存在
4. 初始提交 -> 已完成，提交号 `4ef96e8`

## 关键决定

- `.streamlit*.log` 已加入忽略，日志不进仓库。
- 当前仓库只有本地 Git 历史，尚未绑定远程仓库。

# 本次检查：GitHub 远程绑定

## 计划进度

1. 检查远程配置 -> 已完成，当前没有 `origin`
2. 检查 GitHub CLI -> 已完成，`gh` 可用但未登录
3. 尝试网页登录 -> 未完成，`gh auth login --web` 获取设备码失败
4. 检查 GitHub 连接器 -> 未完成，连接器启动失败

## 关键决定

- 暂不设置不存在的远程地址，避免后续 push 误导。
- 下一步需要用户先完成 `gh auth login`，或提供一个已存在的 GitHub 仓库 URL。

# 本次操作：绑定 GitHub 远程仓库

## 计划进度

1. 检查 GitHub 登录 -> 已完成，账号为 `Aozaki2501`
2. 创建远程仓库 -> 已完成，仓库为 `Aozaki2501/DG-trans`
3. 绑定 `origin` -> 已完成
4. 推送 `main` -> 已完成，当前本地分支跟踪 `origin/main`

## 关键决定

- 远程仓库创建为私有仓库。
- 后续普通提交可直接使用 `git push` 推送。
# 本次审查：本地项目首轮检查（2026-07-10）

## 计划进度

1. 确认目标目录 -> 已完成，仅审查 `D:\DG-trans`
2. 检查核心流程、配置和测试 -> 已完成
3. 调研 GitHub 同类项目 -> 已完成
4. 汇总 TRPG 优化点与风险 -> 已完成

## 关键决定

- 本次只做审查，不修改业务代码。
- 以真实测试和代码行为为依据；优化建议与已确认问题分开。
- 当前 578 个测试全部通过，语法和环境检查通过。
- 主要风险：截断响应未检查、进度损坏会静默重跑、术语表有冲突、缓存指纹不完整。
- TRPG 主线优先补强术语实体记忆、规则数值校验和真实模组回归样本。
- 对比对象：PDFMathTranslate、BabelDOC、Rainman Translate Book、bilingual_book_maker、translation-agent。

# 本次记录：风险、优化与前端重构方向（2026-07-10）

## 计划进度

1. 记录风险与 TRPG 优化方向 -> 已完成，见 `docs/PROJECT_REVIEW.md`
2. 审查现有前端结构 -> 已完成
3. 形成前端重构方向 -> 已完成

## 关键决定

- 前端定位为操作型工作台，主流程收敛为新建任务、执行、校对、档案库。
- 保留 Delta Green 氛围作为视觉点缀，不再让装饰压过任务操作。
- 本次只更新文档，不修改前端业务代码。

# 本次原型：独立前端工作台样例（2026-07-10）

## 计划进度

1. 创建独立原型目录 -> 已完成
2. 搭建四个工作区 -> 已完成
3. 检查桌面和窄屏 -> 已完成

## 关键决定

- 原型位于 `Workspace/projects/project001_frontend_mockup/03_outputs/workbench-sample/`。
- 未修改 `app.py`、`webui/` 和现有业务逻辑。
- 本机缺少 Node，改用纯静态网页完成样例。

# 本次修正：浏览器宽屏尺寸与主按钮（2026-07-16）

## 计划进度

1. 检查宽屏尺寸 -> 已完成，工作区最大宽度收紧到 1440px，宽屏字号为 17px
2. 修正顶部密级条 -> 已完成，移除会拉宽页面的整屏负边距
3. 修正启动按钮 -> 已完成，“开始翻译任务”改为浅色按钮；其他深色主按钮使用白色文字，禁用状态改为浅灰高对比
4. 验证 -> 已完成，2048px 与 760px 浏览器均无横向溢出，579 个测试通过

## 关键决定

- 宽屏只适度放大，不让内容贴近浏览器右边缘。
- 初始启动按钮单独使用浅色次级样式；执行中的主按钮仍保留深绿底和白字。
- 8501 原服务未改动，8502 继续作为新版预览。

# 本次发布：推送当前改动到 main（2026-07-16）

## 计划进度

1. 核对工作树与远程仓库 -> 已完成
2. 运行全量测试 -> 已完成，579 个测试通过
3. 提交并推送 `origin/main` -> 已完成

## 关键决定

- 本轮连续产生的前端、审查文档、术语别名、测试和网页样例作为同一批改动提交。
- 原型运行日志不进入仓库，统一忽略 `*.log`。

# 本次检查：HTML 高保真输出（2026-07-16）

## 计划进度

1. 确认真实项目和样例 -> 已完成，目标仓库为 `D:\DG-trans`
2. 检查现有 HTML、PDF 版式和 GitHub 方案 -> 已完成
3. 形成最小可行方案 -> 已完成，复用纯重绘管线的固定页 HTML
4. 渲染对比和回归验证 -> 已完成，44 个相关测试通过

## 关键决定

- 固定使用 `uploads` 中唯一 PDF 与 `output` 中对应 Holy War HTML，后续只按完整路径操作。
- 普通 HTML 保留为流式阅读版；新增显式“高保真 HTML”，只运行纯重绘管线 Phase A-D，并打包页面图片资源。
- 不把现有整页译文硬套到原坐标，因为旧进度没有文字区域 ID；现有输出只用于视觉对照。
- 暂不新增 PDF.js、pdf2htmlEX、Docling 等依赖：项目已有更贴合当前数据结构的页面重建器。
- 本次只完成检查和方案，没有修改业务代码。

# 本次设计：双 HTML 与无损页面模型（2026-07-16）

## 计划进度

1. 定义高保真与阅读输出契约 -> 已完成
2. 审查现有重绘模型的无损缺口 -> 已完成
3. 统计 Holy War 页面对象复杂度 -> 已完成
4. 确定统一解析与双渲染方案 -> 已完成

## 关键决定

- 两套 HTML 共用一个 canonical page model 和一次按稳定 block ID 保存的翻译结果。
- 高保真版保留原页尺寸与坐标；阅读版允许响应式重排，但所有美术、地图、手稿、卡片和章节分隔必须绑定原页与正文锚点。
- canonical model 必须保存页面框、严格绘制顺序、transform、clip、opacity、blend、字体/字符几何、原始图片资源和语义阅读顺序；未知对象直接报错。
- Holy War 有 439 次图片绘制、138 次遮罩、101 次旋转或斜切图片、1031 个 clip、628 个 group、87 个非普通 blend 和 337 个旋转文字片段，现有 schema v1 无法近乎无损保存。
- 高保真视觉底层优先验证 MuPDF clean-page SVG：移除已映射的原文 text 节点，保留图片、矢量、裁剪和混合效果，再覆盖可搜索中文文字；无法完整映射时停止该页输出。
- 阅读版从同一模型按稳定 visual anchor 输出独立美术资源和语义正文，不再按字数合并原页或删除图片。
- 本次只完成设计和解析审查，没有修改业务代码。

# 本次开发：双 HTML 解析底座（2026-07-16）

## 计划进度

1. canonical schema v2 -> 已完成，保存来源哈希、页面框、绘制顺序、图片/文字/矢量细节与未知对象标记
2. clean-page SVG -> 已完成，只移除 SVG 文字节点，保留页面美术与矢量层，并校验尺寸和哈希
3. 高保真 HTML 入口 -> 已完成，可只运行 Phase A-D；PDF 与 HTML 可同时选择
4. 高保真视觉接入 -> 已完成，HTML 使用 clean-page SVG 作为唯一视觉底层，再覆盖同一 block ID 的中文
5. 图文阅读 HTML -> 已完成，和高保真版共用 block ID 译文与页面美术，可切换图文并排和专注阅读
6. Web 输出入口 -> 已完成，可分别选择 `_typeset.html`、`_reading.html` 和 `_typeset.pdf`
7. 真实页面视觉验收 -> 已完成，桌面、手机、图片放大和打印样式均无横向溢出或资源丢失
8. 验证 -> 已完成，全量 606 个测试通过，语法和差异检查通过

## 关键决定

- 不再用独立图片和 CSS 线条拼原页；高保真版以 MuPDF SVG 保存原绘制结果，避免丢失遮罩、裁剪和旋转。
- 页面结构、翻译页、视觉清单必须页码完全一致；来源哈希、资源哈希或文字清理校验不通过时直接停止。
- 高保真版保留原页坐标和可搜索中文；阅读版响应式重排正文，但每页美术仍和原页、正文区域绑定。
- Holy War 全书核验中，7612 个非空文字片段全部精确映射到绘制记录，439 个图片块全部精确映射到图片资源。
- SVG 的文字节点数量必须与 MuPDF 文字绘制记录一致；图片、矢量、裁剪、遮罩和混合效果不重新猜测或拼装。
- 两种 HTML 的资源包只包含实际引用文件，避免重复打包旧图片资源。

# 本次产出：Holy War 前 12 页双 HTML 测试（2026-07-16）

## 计划进度

1. 解析物理页 1-12 -> 已完成，前 2 页为纯美术页，后 10 页共 65 个可翻译块
2. 块级翻译 -> 已完成，65 个 block ID 全部有译文，遗漏、额外和空译文均为 0
3. 双 HTML 生成 -> 已完成，生成高保真版、图文阅读版及各自资源 ZIP
4. 视觉验收 -> 已完成，桌面、390px 手机和打印样式无横向溢出、文字裁切或资源缺失
5. 验证 -> 已完成，全量 607 个测试通过，语法、差异和 ZIP 完整性检查通过

## 关键决定

- 使用独立目录 `output/Delta_Green_-_Holy_War_APU8157_typeset_p01-12/`，不覆盖旧整本输出。
- 旧进度是页级译文，不能映射到新的 block ID；本次由 Codex 直接完成块级翻译，未调用外部翻译 API。
- 重复运行页眉按现有翻译规则保留 `DELTA GREEN HOLY WAR`；正文、标题和交叉引用使用中文。
- 纯美术页在阅读版中改为居中整页展示，不再保留无内容的空白文字栏。
- 最终交付文件为 `Holy_War_p01-12_typeset.html`、`Holy_War_p01-12_reading.html` 及对应 `.html_assets.zip`。

# 本次复查：高保真测试页与原版对比（2026-07-16）

## 结论

- 当前结果只实现了页面美术和大区块位置保真，文字排版不属于高保真。
- 页眉左右字标被合并为一个红色标题；原字体、颜色、对齐和左右分布均丢失。
- 同一区域中的章节标题、斜体正文和普通正文被压成一个统一样式段落，导致层级、字重、字号和行距消失。
- 原版装饰线仍在原坐标，但中文标题已按普通段落重排，标题与装饰线明显错位。
- 两栏边界大致正确，但段落密度、留白节奏、首行缩进和跨栏视觉平衡与原版差距明显。
- 上一轮验收只验证了资源、溢出和裁切，没有进行原版逐页像素对照，因此“高保真”结论过早。

## 关键决定

- 后续高保真路线必须从 region 级重排改为 span/行级替换，保留每段字体角色、字号、字重、斜体、颜色、基线和装饰锚点。
- 图文阅读版可以继续使用区块级重排；高保真版不能再共用同一套普通段落渲染策略。

# 本次调研：人工汉化 PDF 排版基准（2026-07-16）

## 计划进度

1. 枚举并确认参考文件 `F:\qqDownload\绿色三角洲：全知之眼+v1.1.pdf` -> 已完成
2. 检查字体、字号、栏宽、行距、标题层级和代表页面 -> 已完成
3. 对照当前 Holy War HTML 的数据模型与渲染方式 -> 已完成
4. 制定高保真中文排版改造方案 -> 已完成，尚未修改业务代码

## 关键决定

- 参考文件是 40 页 Adobe InDesign 人工排版；目标应是复用原页美术，并按稳定的中文设计系统重新流排文字，而不是逐字覆盖英文坐标。
- 正文基准为方正书宋系风格 10.5pt、18pt 行距、约 230pt 栏宽、两字首行缩进；当前字号接近，主要差距不是字体大小。
- 标题体系固定为：30pt 开篇标题、20pt 页面主标题、15pt 红色章节标题、12pt 黑色小标题、14pt 运行页眉、9pt 表格正文。
- 必须打包实际 Web 字体；只写字体名称会在当前系统回退到宋体或黑体，无法接近人工版质量。
- 高保真数据模型改为 segment/frame：标题、正文段落、强调、注释、表格单元格各有稳定 ID、样式和几何信息；翻译也按 segment 保存，禁止再压成一个字符串。
- 高保真渲染器分别处理运行页眉、标题、正文栏、表格、卡片和人物数据块；正文通过固定栏框连续排流，无法容纳时直接报告溢出。
- 首轮实现只验证 Holy War 物理第 3 页；通过后再用表格页、卡片页和人物数据页覆盖全部版式类型。

# 本次测试：page content schema v2 与分段渲染

## 计划进度

1. 更新 v2 模型 roundtrip/枚举字段测试 -> 已完成
2. 新增构造数据的同 region 分段稳定性测试 -> 已完成，13 个模型/语义测试通过
3. 新增 HTML 字体角色、字体资源、段落和溢出测试 -> 已完成，等待渲染器实现后复验

## 关键决定

- 测试全部使用构造的 PageStructure/TextLineBBox，不依赖私有用户 PDF。
- 分段契约固定为 46pt display、30pt section、两个 body 段；block ID 使用 region_b0001 起始。
- HTML 角色类按 `font-role-{role}` 检查，JS 禁止通过逐次 `fontSize` 修改静默缩字。

# 本次开发：Holy War 第 3 页中文排版首轮（2026-07-16）

## 计划进度

1. 行级解析与段落切分 -> 已完成，标题、章节标题、正文、页眉、页脚和提示卡各自使用稳定 segment ID
2. 中文字体与层级系统 -> 已完成，内嵌 Noto Serif SC / Noto Sans SC 四套字重并随输出复制许可证
3. 高保真 HTML 重排 -> 已完成，正文按固定双栏连续排流，美术卡片作为硬边界，禁止静默缩字
4. PDF 稳定导出 -> 已完成，等待字体、图片解码和浏览器绘制帧后再输出
5. 第 3 页实页验收 -> 已完成，无溢出、无黑块、中文可搜索，页面尺寸 612×792pt
6. 验证 -> 已完成，全量 614 个测试通过

## 关键决定

- 正文按 10.5pt/18pt、两字首行缩进排版；30pt 开篇标题、20pt 章节标题、14pt 运行页眉分别渲染。
- 翻译进度升级为 schema 2，并绑定原文哈希；segment 内容变化时禁止复用旧的 region 级译文。
- clean-page SVG 继续承担原始美术、纹理和装饰，高保真文字层只负责中文语义与排版。
- 版面放不下时直接报告溢出，不再自动缩小标题或正文掩盖问题。
- 当前首轮只验收开篇双栏页；表格、信息卡和人物数据页仍需按相同模型逐类实现。

# 本次修复：Kali Ghati 网页端 Phase A（2026-07-16）

## 计划进度

1. 复现图片元数据错误 -> 已完成，定位到 PDF 第 2 页
2. 修复图片身份映射 -> 已完成，改用原始像素摘要、变换矩阵和尺寸精确匹配
3. 修复 ActualText 轨迹映射 -> 已完成，按字体、flags 和精确字符起点匹配语义文字与绘制字形
4. 真实 PDF 验证 -> 已完成，38 页、278 个保留图片、627 个文字区域和 2322 个 span 全部解析
5. 页面视觉层验证 -> 已完成，38 页无原文 SVG 全部生成
6. 验证 -> 已完成，全量 619 个测试通过

## 关键决定

- 不再使用 PyMuPDF 的 block number 匹配图片；页外文字会令两套接口编号错位，并可能静默配错后续图片。
- 图片身份哈希必须在 mask 合成前计算，与 `get_image_info()` 的 digest 语义一致。
- PDF ActualText 允许语义字符与实际绘制 glyph 不同；字符码不能作为 trace 身份键，也不使用距离容差。
- 完全等价的重复图片绘制允许共享元数据；相同身份出现冲突元数据时仍直接报错。

# 本次开发：DG 矢量表格与中文排版（2026-07-17）

## 计划进度

1. Kali Ghati 第 14 页结构诊断 -> 已完成，确认表格为矢量网格和可选文字，不是截图
2. 表格语义识别 -> 已完成，支持倾斜网格、页眉/页脚排除和同页多表
3. 翻译前单元格合并 -> 已完成，多行碎片按列合并后整体翻译
4. HTML 表格重建 -> 已完成，输出真实 table、7 列表头、斑马纹和跨列注释
5. 标题与正文修复 -> 已完成，正文恢复 10.5pt/18pt 流排，标题尊重原字号且不裁切
6. 实页验收 -> 已完成，浏览器布局问题 0，预览 HTML/PNG 已输出
7. 验证 -> 已完成，全量 625 个测试通过

## 关键决定

- PDF 原有矢量表格只用于识别几何和列锚点；中文输出使用一个完整的语义 HTML table，不再逐格绝对定位。
- 同一单元格跨多个 PDF region 时先合并原文再翻译，避免“半句翻译 + 半句翻译”的错误拼接。
- 同页多张 DG 表格按连续表格区段分别重建；`>>` 标题、运行页眉和页码不会被吸入表格。
- 高保真正文继续使用 10.5pt/18pt 双栏流排；定位标题使用原 PDF 字号上限并扩大水平容器，禁止裁切和强制放大。
- 网页端默认使用随输出打包的 `DG Noto Serif SC`，不再被系统字体检测错误替换。

# 本次修复：Dead Letter 上标文字 trace（2026-07-17）

## 计划进度

1. 定位失败文件和页面 -> 已完成，Dead Letter 第 15 页的上标 `3/16`
2. 对比 raw span 与 text trace -> 已完成，字体、字号和原点一致，仅 raw span 多出上标位
3. 最小修复 -> 已完成，匹配字体 flags 时只排除 PyMuPDF 派生的 superscript 位
4. 原 PDF 验证 -> 已完成，62 页、412 个文字区域、4618 个 span 全部解析
5. 验证 -> 已完成，全量 626 个测试通过

## 关键决定

- superscript 是 raw 文本抽取根据行几何派生的语义位，不一定存在于绘制 trace，不能作为两者身份键。
- 字体名称、粗体/斜体等其余 flags 和精确字符原点仍严格匹配；不跳过 span，不使用距离容差。

# 暂停记录：Dead Letter 高保真 HTML（2026-07-17）

## 当前进度

1. 前 16 页对比 -> 已完成
2. 五个问题的回归测试 -> 已完成
3. 生产代码修复 -> 未开始
4. 当前定向测试 -> 9 个通过，5 个预期失败

## 关键决定

- 最终目标是《全知之眼》式 HTML，包括中文页眉、稳定双栏、正确标题层级、卡片和时间线结构。
- 只提交代码、测试、字体资源和 `docs/TYPESET_HTML_PROGRESS.md`。
- 原 PDF、标杆 PDF、截图和生成预览保留在本机，不上传。
# 本次整合：DG-trans 回归正式仓库

## 计划进度

1. 对比两个 GitHub 仓库 -> 已完成，确认两边没有共同 Git 历史
2. 导入 DG-trans 最新产品代码 -> 已完成，排除公司 Codex 前端演示项目
3. 修复新仓库已有测试失败 -> 已完成，语义分栏、页眉翻译、强调色标题、时间线标题和艺术页固定文字已修正
4. 验证新版 Web -> 已完成，桌面与手机页面正常，浏览器无报错
5. 全量测试 -> 已完成，631 个测试通过，语法检查通过
6. 提交与发布 -> 已完成，已推送整合分支并创建草稿 PR #16

## 关键决定

- 旧仓库继续作为正式仓库，使用独立整合分支导入新代码，不合并两段无共同祖先的 Git 历史。
- 保留产品代码、测试、字体和必要文档，不带入公司 Codex 生成的前端演示项目与截图。
- 高保真 HTML 的明确测试失败必须修复后才能提交，不以“新仓库原本如此”为理由跳过。
# 本次更新：拉取项目并同步 README

## 计划进度

1. 检查本地状态并拉取远端 -> 已完成，`main` 已快进到 `1eebb89`
2. 对比最近 5 个提交的功能变化 -> 已完成
3. 更新 README -> 已完成，补充近期变化并纠正已停用输出说明
4. Review 与验证 -> 已完成，573 个测试通过，文档差异检查通过

## 关键决定

- README 只记录当前真实可用功能，不把尚未正式接入的时间线、备团清单和模组结构文件列为常规输出。
- 保留拉取前 `context.md` 中已有的项目检查记录，不覆盖用户内容。

# 本次检查：项目拉取与用途确认

## 计划进度

1. 检查 Git 状态并拉取最新 -> 已完成，远端已是最新
2. 阅读 README、依赖和入口文件 -> 已完成
3. 判断项目类型和主要用途 -> 已完成

## 结论

- 当时当前分支是 `main`，最新提交为 `8028ae5 Add quality check retranslate and fix subheading extraction`。
- 这是一个本地运行的英文 TRPG 文档中文翻译工具，主要面向 Delta Green 这类 PDF，也支持 Markdown 和 Word。
- 主要入口是 `app.py` 的 Streamlit Web 界面，以及 `translate_pdf.py`、`translate_md.py`、`translate_docx.py` 三个命令行入口。

## 关键决定

- 本次只检查和记录项目现状，不修改业务代码。

# 本次更新：New Age 高保真 HTML 修正

## 计划进度

1. 对齐英文 PDF、中文风格参考与 15 页 HTML -> 已完成
2. 修复堆叠纸片和时间线排版 -> 已完成
3. 离线重建并逐页截图检查 -> 已完成
4. 全量验证 -> 已完成，634 个测试通过

## 关键决定

- 《融合》只作为中文排版风格参考，不参与《新时代》的内容逐页映射。
- 修复生成器的通用结构识别，不对单个 HTML 写固定页补丁。
- 时间线按左右栏重组日期与事件；堆叠纸片保持独立文字区域。

# 本次更新：New Age 字体与段落精修

## 计划进度

1. 对照英文原版整理字体角色与标题颜色 -> 已完成
2. 恢复段落间距、首行缩进和标题避让 -> 已完成
3. 修复同一区域正文误删主标题的问题 -> 已完成
4. 重建并逐页截图检查 15 页 -> 已完成
5. 全量验证 -> 已完成，639 个测试通过

## 关键决定

- 中文字体按原版的展示体、几何体、打字机体和正文体分工，不强行使用单一字体。
- 主标题沿用原版黑色，章节标题沿用原版橙红色。
- 标题与正文分段排版，正文不再穿过标题位置。

# 本次更新：《融合》字体标尺与首页汉化

## 计划进度

1. 提取《融合》中文字体和字号 -> 已完成
2. 映射正文、黑色一级标题、红色二级标题和页眉 -> 已完成
3. 恢复首页中文模组名与说明文字 -> 已完成
4. 重建并检查 15 页 -> 已完成
5. 全量验证 -> 已完成，640 个测试通过

## 关键决定

- 黑色一级标题使用完整授权的方正字迹-牟氏美隶简体。
- 红色二级标题和作者行使用完整授权的方正兰亭刊黑简体。
- 正文使用完整 FandolSong 10.9 pt、17 pt 行距；特殊文字使用 FandolKai。
- 四款字体均转换为 WOFF2 随输出打包；Noto 字体只保留为缺字备用。
- 页眉最终统一为两侧 `//` 格式，使用方正字迹-牟氏美隶简体 11 pt；左右分别贴齐正文外侧边缘，文字靠近页眉下边线。

# 本次更新：清理 glossary 末尾书名号并准备推送

## 计划进度

1. 移除 glossary 末尾书名号 -> 已完成
2. Review 与相关测试 -> 已完成，640 个测试通过，差异检查通过
3. 提交并推送到 `main` -> 已完成，提交 `e157820` 已推送

# 本次更新：按 PROJECT_REVIEW 修复静默正确性问题

## 计划进度

1. 先处理（P0）8 项 -> 已完成
2. 随后处理（P1）9 项 -> 完成 7 项；B7（CI/依赖/LICENSE）未做，B8（前端拆分）按约定跳过
3. 性能 2 项 -> 已完成
4. 补测试 -> 新增 9 个测试文件、102 条测试
5. 全量验证 -> 已完成，742 个测试通过
6. 复查排版产物并记录待修问题 -> 已完成，写入 docs/OPTIMIZATION_PROGRESS.md 第六节

## 关键决定

- 截断阈值取 0.25，不采用文档建议的 0.45。实测 31 个健康页面比值区间为 0.383-0.542，
  按 0.45 会有 29/31 个健康页被判失败并被挡在输出之外。0.25 留约 1.5 倍余量，
  仍比原来的 0.15 灵敏约 1.7 倍；测试用 0.383 做回归护栏防止再次调高。
- 术语表采用大小写敏感匹配，通过 surface 字段让 AC 与 regex 两条路径结果一致；
  同一英文词条译名冲突时 load_glossary 直接报错并给出行号，逐字重复的行不算冲突。
- 单价改为可配置并绑定 base_url，未配置时显示"未配置单价"而不是 ¥0.000。
- 进度指纹不记录 max_workers。同一批改动已让提示词与并发解耦（组上下文改为
  上一组的原文尾部），记录 workers 只会白白丢弃已付费的译文。
- 提示缓存写入改为延迟落盘，mark_completed / mark_failed / clear_* 仍强制写盘。
- Owlshead Mountain 冲突保留"枭首山"、删除音译"奥尔斯海德山"，术语表现为 977 条。

## 遗留问题

- 排版层仍有与本轮同类的静默问题：报告 failed_regions 为 0，但第 4 页整页退化成逐行
  翻译（40 个区域 vs 其他页 2-13 个），跨栏段落被切开后译文里出现 [...] 占位符。
  详见 docs/OPTIMIZATION_PROGRESS.md 第六节 T1-T6，修 T1 需改 semantic_analyzer
  的区域分割逻辑，会使现有进度缓存失效，建议单独开分支。

# 本次更新：修排版层静默错误 T1 + T2（2026-07-26）

## 计划进度

1. 核对真实产物定位 T1 根因 -> 已完成，发现文档的根因判断是错的
2. Phase A 相邻区域合并 -> 已完成，core/page_structure.py
3. Phase B 悬挂缩进感知切分 -> 已完成，core/semantic_analyzer.py
4. T2 把 [...] 判为硬失败 -> 已完成，core/translation_validation.py
5. 全量验证 -> 已完成，760 个测试通过（基线 742，新增 18 条）

## 关键决定

- **订正文档对 T1 根因的判断**。文档说根因在 semantic_analyzer 的 _segment_region
  按左边缘精确相等聚合。实测第 4 页 40 个区域里 39 个在 Phase A 就已是"一行一个区域"，
  _segment_region 拿到的输入已经是碎的。真正成因分两层，两层都要修：
  Phase A 的 extract_text_regions 原样采纳 PyMuPDF 的 text block 划分；
  Phase B 的 _starts_new_segment 用"比基线深 = 新段"判断，在悬挂缩进块里正好判反。
- 区域合并保持 PyMuPDF 原始顺序，不按 y 重排——重排会打乱分栏页的阅读顺序。
- 合并三个条件：垂直间隙 ≤ 0.6×行距、水平投影有重叠、字号差 ≤ 12%。
  另加一条：起点比前一区域更靠左的，视为新的缩进层级（项目符号列表接正文），不合并。
- 悬挂缩进判定要求"浅于基线的行后面跟着更深的续行"。只靠"存在更浅的行"会把
  普通首行缩进段落和末尾短行误判成悬挂缩进。
- 进度缓存失效不做迁移（已确认）。合并后源文本本身变了，按源文本哈希基本迁不上。
- [...] 在两份真实产物的原文里出现 0 次，判为硬失败不会误伤原文。
  读取侧同样过滤，让旧进度里已存的占位符译文重译而不是直接报错。

## 实测效果

- New Age（66 页）：区域 511->381，翻译块 1043->952
- Presence（34 页）：区域 237->177，翻译块 506->506（区域合并但段落切分不变）
- 第 4 页：40 个区域 -> 8 个，翻译块 41 -> 16，六个标签段落各自成段
- 第 3 页：被切断的跨栏句子合并进同一个翻译块，[...] 的成因消失
