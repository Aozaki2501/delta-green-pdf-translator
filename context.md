# 当前计划

1. 整理项目现状 -> 对齐功能入口、测试现状、主要流程
2. 检查风险和隐患 -> 找出会误导使用或增加维护成本的问题
3. 收口无用开发内容 -> 先清理明显过期的脚本说明和重复文档

# 当前进度

- 已确认主线功能：PDF / Markdown / Word 输入，输出 HTML / Word / Markdown，另有独立的 `_typeset.pdf` 流程
- 已确认测试现状：`.venv` 下 `pytest` 487 通过；系统 Python 直接跑 `pytest` 失败，说明实际工作流依赖项目虚拟环境
- 已完成整理：`test_setup.py` 改为检查 Streamlit；`docs/GUIDE.md` 改成精简版；`README.md` 的开发自检命令改为使用 `.venv`
- 已完成修复：`start_web.ps1` 不再在启动时强制下载 Chromium；Web 端导出 `_typeset.pdf` 时会按需提示加载浏览器内核插件并显示进度
- 已完成删减：移除 `.kiro` 开发配置、根目录历史计划、`docs/plans/*`、旧设计/路线图/任务/测试说明；清理本地 `dist/`、`scratch/` 和缓存目录

# 关键决定

- 先不碰主翻译逻辑，只修会误导使用和维护的旧内容
- `README.md` 作为唯一完整使用说明，`docs/GUIDE.md` 只保留最短入口
- 输出目录、打包产物、实验样本先只标记为可清理对象，不直接删除用户数据
- 浏览器内核改为按需安装：普通 Web 启动不依赖 Chromium，只有纯重绘 PDF 流程才触发加载
- 删除原则：保留运行入口、测试、用户说明、术语表和真实输出；删除无引用的开发过程材料和本地临时产物

# 本次检查：The_Borellus_Connection_linked (1).pdf

## 计划进度

1. 定位 PDF 和项目转换入口 -> 已完成
2. 检查 PDF 文本层和页面类型 -> 已完成
3. 判断 Word/HTML 兼容性 -> 已完成

## 结论

- PDF 共 421 页，文字层存在；不是纯扫描件。
- 几乎每页都有整页图片层，普通 Word/HTML 路线会忽略图片层，主要风险不在图片。
- 当前普通 Word/HTML 路线不能直接放心跑整本：目录识别过宽，13 页被当作目录页，其中多页正文会被截短。

## 关键决定

- 暂不改代码，只给兼容性结论。
- 后续若要支持这本 PDF，优先修 `core/extractor.py` 的目录识别和目录抽取，确保不漏正文。

# 本次改动：目录误判修复与三栏输出

## 计划进度

1. 收紧目录识别 -> 已完成，误判目录页从 13 页降到 4 页
2. 支持三栏阅读布局 -> 已完成，三栏页会标记为 `three_columns`
3. Word 分栏选项扩展 -> 已完成，可选单栏、双栏、三栏
4. 验证 -> 已完成，`.venv` 下 496 个测试全部通过

## 关键决定

- 普通 HTML/Word 走阅读版三栏复原，不追求还原整页背景图和贴纸层。
- 真正的整页视觉复原仍应走 `_typeset.pdf` 独立流程。
- 不用兜底猜测：目录识别只认明确 Contents/Index 或大量点线页码。

# 本次检查：卡片类内容识别

## 计划进度

1. 统计全书结构标记 -> 已完成
2. 抽查截图页 023 / 031 -> 已完成
3. 判断兼容性 -> 已完成

## 结论

- 当前能识别一部分卡片：全书识别出 136 个 `[CARD]`，覆盖 105 页。
- 当前不能完整识别属性小卡：`[STAT_BLOCK]` 为 0。
- 截图页 023 只识别了顶部黄色纸条，灰色属性块和白色刷底说明块多为普通正文。
- 截图页 031 识别了底部 `SALT-CUT INVESTIGATORS` 卡片。

## 关键决定

- 普通翻译不会因此漏文字，但卡片语义分类还不够完整。
- 若要更像原版排版，需要新增属性块、窄栏说明块、纸条块的几何识别规则。

# 本次改动：FoDG 属性卡识别

## 计划进度

1. 定位样本页 -> 已完成，原书页 040 对应 PDF 第 45 页
2. 新增属性卡分组 -> 已完成，支持 Courier 属性卡和 Futura 属性卡
3. 防止误伤技能表 -> 已完成，纯技能名/数值列表不会被当成属性卡
4. 验证 -> 已完成，`.venv` 下 499 个测试全部通过

## 结论

- 第 45 页现在能识别出 2 个 `[STAT_BLOCK]`：`MING YUAN` 和 `Large Poppet`。
- 全书当前识别出 64 个 `[STAT_BLOCK]`，覆盖 54 页。
- 普通 `[CARD]` 当前为 126 个。

## 关键决定

- 属性卡必须有明确标签，例如 `Abilities:`、`General Abilities:`、`Hit Threshold:`、`Attack:`、`Armor:`。
- 只靠 `Drive 5`、`Firearms 12` 这类技能表数字不判定为属性卡。

# 本次检查：30 页输出表格误判

## 计划进度

1. 检查输出审计 -> 已完成，1-30 页输出中第 7 页失败，实际完成 26 页
2. 定位表格误判 -> 已完成，第 23-26 页有等宽正文被误判成表格
3. 收紧表格识别 -> 已完成，正文里的字体切换、断词、普通 `name` 不再触发表格
4. 验证 -> 已完成，`.venv` 下 501 个测试全部通过

## 结论

- 原输出确实有问题：部分普通段落被包成 Markdown 表格，Word/HTML 会被拆开。
- 当前源码重新抽取前 30 页，已没有误包出来的管道表格行。
- 第 7 页是独立翻译失败，不是排版识别问题。

## 关键决定

- 表格识别只接受强信号：管道符、分隔线、明确表头，或有明显空隙并稳定对齐的三列网格。
- FoDG 的普通等宽说明文字不再按表格处理。

# 本次检查：完整 Word/HTML 输出

## 计划进度

1. 检查完整输出审计 -> 已完成，421 页中完成 396 页，7 页翻译失败
2. 扫描 HTML/Word 标记泄漏 -> 已完成，发现目录围栏 ` ```toc ` 泄漏
3. 扫描表格误判 -> 已完成，发现第 102、150 页等正文被旧规则拆成表格
4. 修复规则并验证 -> 已完成，`.venv` 下 504 个测试全部通过

## 结论

- 当前已生成的完整 Word/HTML 不是最终可放心版：缺 7 页，且保留了生成时的目录围栏和表格误拆问题。
- 当前源码已修复：全书重新提取后没有正文误拆成 Markdown 表格。
- 还有少量英文卡片标题残留，例如 `Scene Type`、`Lead-Ins`、`Lead-Outs`、`Local Contacts`。

## 关键决定

- `10+ hours`、`4+ Health damage` 这类普通加号不再触发表格识别。
- HTML/Word 导出会自动修正挤在同一行的 ` ```toc ` 目录围栏。
- 现有输出文件需要重新生成才会吃到本次修复。

# 本次修复：失败页与重翻页同时执行

## 计划进度

1. 检查界面执行状态 -> 已完成，实际生成了带后缀的新输出
2. 定位未重翻原因 -> 已完成，`只重试失败页` 覆盖了 `重翻页码`
3. 修复任务页选择 -> 已完成，失败页与重翻页会合并执行
4. 验证 -> 已完成，`.venv` 下 504 个测试全部通过

## 结论

- 之前同时勾选 `只重试失败页` 并填写 `102,150` 时，102 和 150 会被清掉旧译文但没有进入翻译队列。
- 现在会一起处理失败页和手动重翻页。

## 关键决定

- 重翻页码优先级不能被 `只重试失败页` 吃掉。

# 本次新增：Workspace 全局工作台

## 计划进度

1. 检查现有规范文件 -> 已完成
2. 创建全局工作台文档 -> 已完成
3. 创建复利与踩坑日志 -> 已完成
4. 创建新项目 SOP -> 已完成
5. 接入 `AGENTS.md` -> 已完成

## 结论

- 已创建 `Workspace/00_GLOBAL_WORKBENCH.md`，作为总入口。
- 已创建 `Workspace/01_COMPOUND_LESSONS_LOG.md`，用于沉淀错误和复用方法。
- 已创建 `Workspace/02_NEW_PROJECT_SOP.md`，规定新项目目录和命名。
- 已创建 `Workspace/projects/README.md`，作为项目容器说明。

## 关键决定

- 新项目统一放在 `Workspace/projects/projectNNN_项目名称/`。
- 项目资料、过程文件、输出和日志都必须放入对应项目文件夹。
- `AGENTS.md` 已加入全局工作台入口规则。

# 本次优化：DeepSeek 缓存命中

## 计划进度

1. 分析后台 token 结构 -> 已完成，缓存命中率约 37.4%
2. 降低默认并发 -> 已完成，默认并发从 32 改为 8
3. 固定 system prompt -> 已完成，页内术语改放 user prompt 末尾
4. 验证 -> 已完成，`.venv` 下 506 个测试全部通过

## 结论

- 之前每页术语会进入 system prompt，导致 system prompt 每页变化，不利于 DeepSeek 前缀缓存。
- 现在 system prompt 固定，页内术语仍会发送，但不再破坏 system prompt 缓存。
- 默认并发降低后，更利于缓存先写入再被后续请求命中，也能减少 API 报错。

## 关键决定

- 缓存友好优先：稳定规则放 system，变化内容放 user。
- 默认并发使用 8；需要速度时可手动调高。

## 后续计划

1. 缓存预热 -> 先用 1 个请求翻译首页或首个待翻页，完成后再启动并发，验证 DeepSeek 缓存命中率是否提升。
2. 固定核心术语表 -> 从 `glossary.tsv` 中抽取 50-100 个高频术语，作为稳定前缀发送，验证命中率和术语一致性。

# 本次优化：DeepSeek 缓存预热与核心术语前缀

## 计划进度

1. 缓存预热 -> 已完成，PDF 并发前先翻译首个真实待处理页
2. 固定核心术语表 -> 已完成，从本次源文本命中的术语中选最多 80 个高频项，放到 user prompt 开头
3. 缓存隔离 -> 已完成，`PROMPT_VERSION` 升级到 v14，避免旧进度缓存混用
4. 验证 -> 已完成，`.venv` 下 510 个测试全部通过

## 结论

- system prompt 继续固定，不再塞入每页变化的术语。
- user prompt 开头现在有稳定核心术语前缀；页内补充术语仍放末尾，重复项会去掉。
- PDF 并发前会先预热 1 页，再启动并发，给 DeepSeek 前缀缓存先写入机会。

## 关键决定

- 核心术语只从本次实际源文本命中的术语里选，不直接塞完整术语表。
- 提示词结构变化必须提高 `PROMPT_VERSION`，让旧译文进度明确失效。

# 本次调研：GitHub 同类项目优化灵感

## 计划进度

1. 查找同类项目 -> 已完成，重点看 PDF 翻译、文档翻译、PDF 解析三类
2. 提炼可借鉴方向 -> 已完成，优先选择不破坏当前主线的优化点

## 结论

- 最接近的是 PDFMathTranslate、BabelDOC、DocuTranslate、LLM_PDF_Translator。
- 最值得借鉴的是：可视化质量检查、术语候选确认、任务级 manifest 校验、解析后端显式切换。
- 不建议直接追求完整 PDF 原版复刻；这会明显扩大复杂度，应继续保留阅读版和 `_typeset.pdf` 两条路线。

## 关键决定

- 新优化应优先围绕“减少失败返工”和“让用户更容易判断结果可靠”。
- 外部解析器只能作为显式高级模式，不做静默兜底。

# 本次新增：质量检查台

## 计划进度

1. 新增质量检查逻辑 -> 已完成，统一检查失败页、缺译文、疑似未翻译、疑似截断、英文残留和术语遗漏
2. 接入 Web PDF 流程 -> 已完成，翻译结束后直接显示质量检查汇总和问题页
3. 接入 CLI PDF 流程 -> 已完成，生成 `_quality_report.md`
4. 验证 -> 已完成，`.venv` 下 513 个测试全部通过

## 结论

- Web 端现在会显示检查页数、有译文页数、失败页、待检查问题和术语遗漏。
- 每次 PDF 翻译会生成质量检查报告，供后续复查。
- `_quality_report.md` 已归类为内部报告，不再被档案库当成最终成品。

## 关键决定

- 疑似未翻译和疑似截断仍按现有规则拦截。
- 英文残留和术语遗漏只作为待校对提示，不直接阻止导出。
- 质量检查里的术语命中使用翻译时同一个匹配器，避免检查结果和实际 prompt 不一致。

# 本次新增：术语候选确认

## 计划进度

1. 汇总疑似未收录专名 -> 已完成，按出现页数和次数排序
2. 接入 Web PDF 流程 -> 已完成，翻译结束后显示前 30 个候选术语
3. 生成候选文件 -> 已完成，输出 `_glossary_candidates.md` 和 `_glossary_candidates.tsv`
4. 验证 -> 已完成，`.venv` 下 515 个测试全部通过

## 结论

- Web 端现在能看到高频疑似专名候选。
- 候选 TSV 的中文列默认留空，直接加载会被跳过，不会污染术语表。
- 候选报告已归类为内部报告，不会被档案库当成最终成品。

## 关键决定

- 只做候选提示，不自动写入 `glossary.tsv`。
- 候选来自提取后的源文，仍需人工确认后再加入术语表。

# 本次新增：直观校对与运行报告

## 计划进度

1. 问题页对照校对 -> 已完成，质量检查的问题页可直接查看英文原文和中文译文片段
2. 运行清单 -> 已完成，输出 `_manifest.json`，记录输入、设置、结果和报告文件
3. 效果报告 -> 已完成，输出 `_run_report.md`，Web 端也显示缓存命中率、API 调用和平均每页费用
4. 验证 -> 已完成，`.venv` 下 518 个测试全部通过，并编译检查 `app.py`

## 结论

- 问题页不再只是页码列表，能直接在界面里对照校对。
- 每次 PDF 翻译会额外生成运行清单和效果报告。
- `_manifest.json` 和 `_run_report.md` 已归类为内部报告，不会被档案库当成最终成品。

## 关键决定

- 保留方便直观的信息，不继续加入高级解析、术语在线编辑等复杂功能。
- 效果报告只展示能帮助判断本次运行是否顺利的数字。

# 本次优化：项目体积清理

## 计划进度

1. 定位空间占用 -> 已完成，主要来自 `.venv`、`uploads`、`.git`、`output`
2. 上传去重 -> 已完成，新上传文件按 SHA256 复用，避免同一文件重复保存
3. 清理本地体积 -> 已完成，删除重复上传、旧 progress、Python 缓存，重建 `.venv`，压缩 Git 对象
4. 验证 -> 已完成，`.venv` 下 519 个测试全部通过

## 结论

- 项目目录从约 1.35GB 降到约 732MB。
- `uploads` 从约 490MB 降到约 126MB。
- `.git` 从约 184MB 降到约 0.8MB。
- `.venv` 从约 562MB 降到约 461MB。

## 关键决定

- 保留 `output/` 成品，不删除历史输出。
- 只删除重复上传、旧 progress 和可再生成缓存。
- `logs/` 已加入 `.gitignore`。
