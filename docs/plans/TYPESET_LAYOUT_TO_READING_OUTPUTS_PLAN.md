# 重绘 PDF 排版能力普及到普通输出计划

1. 建立公共适配层 -> 把重绘 PDF 的页面语义转成普通 HTML/Word 能理解的布局标签。
2. 接入普通 PDF 输出链路 -> CLI、Web、离线重排都使用同一份布局结果。
3. 增加测试 -> 验证特殊布局不被覆盖，普通布局能被语义结果修正。
4. 更新阶段记录 -> 在 `context.md` 记录本次决定和测试结果。

## 进度

- 已完成：新增 `core/layout_adapters.py`，负责把 typeset 语义页转换为普通输出布局上下文。
- 已完成：CLI、Web、离线重排都已接入同一份布局结果。
- 已完成：新增测试，确认普通布局可被语义结果修正，`toc/handout/character/art` 等特殊布局不会被覆盖。
- 已验证：相关测试 56 个通过；全量测试 441 个通过。
