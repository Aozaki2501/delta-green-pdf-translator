# 测试用例清单

这份文档用于手动验收和回归测试。每次修改 PDF 提取、术语表、断点续跑、网页端或 Word 输出后，都可以按这里检查一遍。

## 测试素材

建议准备：

- 一个 3-5 页、带文本层的 TRPG PDF。
- 一个典型双栏排版的 Delta Green 风格 PDF 页面。
- 一个包含通栏图片/标题区、下方双栏正文的页面。
- `glossary.tsv` 中至少包含：

```text
绿色三角洲	Delta Green
圣阉宗	The Skoptsi
斯克布提	Skoptsi
```

注意：不要把真实 API Key 放进截图、提交记录或共享日志里。

## 1. 环境编译检查

操作：

```powershell
python -m py_compile translate_pdf.py app.py
```

预期结果：

- 命令正常结束。
- 没有语法错误。

## 2. 网页端能正常启动

操作：

```powershell
python -m streamlit run app.py
```

然后打开终端里显示的本地网址。

预期结果：

- 网页能正常打开。
- 左侧栏能看到模型、并发线程、页码范围、重翻页码、提取预览、Word 版式等设置。
- 如果项目里有 `glossary.tsv`，页面会提示默认使用该术语表。

## 3. PDF 提取预览

操作：

1. 上传一个带文本层的 PDF。
2. 勾选 `显示提取预览`。
3. 选择一个已知有正文的页码。

预期结果：

- 页面显示提取后的文本。
- 不会调用翻译 API。
- 页眉、页脚、单独页码大多已经被过滤。
- 双栏正文的顺序基本符合阅读顺序。

## 4. 图片页或空文本页预览

操作：

1. 预览一个几乎全是图片、或没有文本层的页面。

预期结果：

- 页面提示没有提取到正文文本。
- 程序不会崩溃。

## 5. 术语最长匹配

操作：

1. 确认 `glossary.tsv` 里同时有：

```text
圣阉宗	The Skoptsi
斯克布提	Skoptsi
```

2. 翻译或预览包含 `The Skoptsi` 的页面。
3. 下载术语命中报告。

预期结果：

- 报告中应该出现 `The Skoptsi -> 圣阉宗`。
- 不应该因为重叠匹配，把同一个 `The Skoptsi` 又拆成 `Skoptsi -> 斯克布提`。

## 6. 术语命中报告

操作：

1. 在网页端用术语表运行一次翻译。
2. 翻译完成后下载 `*_glossary_report.md`。

预期结果：

- 报告包含这些部分：
  - `汇总`
  - `逐页命中`
  - `疑似未收录专名`
- 每条命中都显示英文原词、中文译名、来源页。
- 报告文件也会生成在 `output/` 目录中。

## 7. 断点续跑

操作：

1. 翻译第 1-3 页。
2. 用同一个 PDF、同一个模型、同一个术语表、同一个页码范围再运行一次。

预期结果：

- 程序会加载已有的 `*.progress.json`。
- 已完成的页面会跳过或复用。
- 除非指定重翻，否则不会重新翻译已完成页。

## 8. Progress 指纹不匹配

操作：

1. 使用某个模型或术语表翻译第 1-3 页。
2. 换一个模型，或上传不同术语表。
3. 不勾选 `允许复用设置不匹配的旧进度`，再次运行。

预期结果：

- 网页会提示 progress 与当前设置不一致。
- 原有 progress 文件不会被删除。
- 默认不会复用旧译文。

## 9. 指定页重翻

操作：

1. 先完成第 1-5 页翻译。
2. 在 `重翻页码` 中输入：

```text
2, 4-5
```

3. 再次运行。

预期结果：

- 第 2、4、5 页会从当前进度中清理出来并重新翻译。
- 其他已完成页继续复用。
- 网页会提示哪些页被标记为重翻。

## 10. Word 默认输出版式

操作：

1. 输出格式选择 `Word`。
2. Word 版式保持默认。
3. 翻译一个小范围页面。
4. 打开生成的 `.docx`。

预期结果：

- 正文字号是 12pt。
- 行距是 1.5。
- 正文默认双栏。
- 页眉显示左右标题。
- 页脚有页码。
- 二级标题是红色，并且比正文醒目。

## 11. Word 自定义版式

操作：

在 `Word 版式` 中设置：

- 正文字号：`10.5`
- 正文行距：`1.2`
- 正文分栏：`1 栏`
- 页眉左侧：`测试左`
- 页眉右侧：`测试右`

然后生成 Word。

预期结果：

- Word 正文样式符合设置的字号和行距。
- 正文变成单栏。
- 页眉显示 `// 测试左 //` 和 `// 测试右 //`。

## 12. Markdown 输出

操作：

1. 输出格式选择 `Markdown`。
2. 翻译带有小标题的页面。

预期结果：

- 会生成 `.md` 文件。
- 文件中有阅读页注释，例如：

```markdown
<!-- Reading Page 1; Source PDF Pages: ... -->
```

- 识别到的小标题会尽量转换成 Markdown 标题。

## 13. 输出文件被占用

操作：

1. 生成一个 Word 文件。
2. 不关闭 Word，保持这个 `.docx` 文件被占用。
3. 再次生成 Word 输出。

预期结果：

- 程序不会因为 `PermissionError` 崩溃。
- 如果原文件被占用，会创建一个带随机后缀的新输出文件。

## 14. 完成后打开 output 文件夹

操作：

1. 勾选 `完成后打开 output 文件夹`。
2. 完成一次翻译。

预期结果：

- Windows 文件资源管理器会打开项目的 `output/` 目录。
- 如果自动打开失败，网页会显示警告，而不是崩溃。

## 15. CLI 小范围翻译

操作：

```powershell
python translate_pdf.py "sample.pdf" --api-key sk-xxx --glossary glossary.tsv --format word --start 0 --end 1
```

预期结果：

- 生成 Word 输出。
- 生成 progress 文件。
- 生成术语命中报告。
- 终端不会打印 API Key。

## 16. API Key 不泄漏

操作：

1. 在网页端和命令行里故意触发一些错误，比如缺少文件、页码不对。
2. 检查终端输出、网页提示、生成的 Markdown、Word、progress 文件。

预期结果：

- API Key 不会被打印。
- API Key 不会写入任何输出文件。

## 17. 不删除旧输出和旧进度

操作：

1. 确认 `output/` 中已有旧文件和旧 progress。
2. 运行新的翻译和预览。

预期结果：

- 旧输出文件不会被删除。
- 旧 progress 文件不会被自动删除。
- 需要新文件名时，会生成带随机后缀的新文件。

## 快速发布检查

提交功能前，至少检查：

- [ ] `python -m py_compile translate_pdf.py app.py`
- [ ] 网页端能正常启动。
- [ ] 已知双栏页面的提取预览顺序正常。
- [ ] Word 输出能打开。
- [ ] 启用术语表时会生成术语命中报告。
- [ ] 没有打印或提交 API Key。
