# 纯重绘 PDF 修复计划

## 目标

让 `_typeset.pdf` 至少做到：图片正常显示、固定页眉页码不乱跑、正文不互相压住、深色背景文字可读。

## 当前进度

- 已修复：PDF 图片统一转 PNG，避免浏览器导出时丢图。
- 已修复：带 soft mask 的图片会合成透明遮罩，避免整页黑块。
- 已修复：旧的 JPX/JPEG2000 图片缓存会触发重新提取。
- 已修复：页眉左右固定，`God's Law` 和 `Delta Green` 不再挤在一起。
- 已修复：页码固定在底部页码框，不再受正文缩进影响。
- 已修复：深色背景上的重复黑白叠字会去重，保留可见的亮色文字。
- 已修复：固定文本框内取消正文缩进和大行距。
- 已修复：相邻同栏正文块会合并成连续文本流，减少块与块互相压住。
- 已修复：导出 PDF 前执行文本框自动缩字，防止译文溢出到下一块。

## 下一步

- 修复 Presence 类页面：重叠英文文本块导致译文重复、挤压。
- 修复中文正文排版：按人工排版参考使用版心/双栏文本流、首行缩进、合理行距，而不是把中文塞回每个英文碎片框。
- 修复标题、小标题、页眉、页码的固定规则。
- 检查真实中文译文的字体嵌入和可读性。
- 检查整本范围的失败页和失败文本块。
